# Receipt — machine-local-home-path sweep V403

Replaced machine-local user home paths (`Users\\<user>` / `C:/Users/<user>` / `/Users/<user>`) with portable placeholders `<LIBRARY_ROOT>`, `<WORKSHOP>`, `<LOCALAPPDATA>`, `<APPDATA>`, `<USERPROFILE>` across the release tree.

JSON: `RECEIPT_MACHINE_LOCAL_HOME_PATH_SWEEP_V403.json`.
