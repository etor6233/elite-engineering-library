# V402317 — T2809 operación local conectada

PROVEN_LOCAL en LIBRARY_INFRASTRUCTURE_LOCAL_FIXTURES. Perfil116packs/1583archivos;
biblioteca205packs/2300blocks:1996AUTHORED159ADAPTED145VERBATIM.
Seis bloques nuevos de glue/test/guía, 7archivos previos modificados,
17archivos existentes seleccionados y 1553previos exactos.

El mismo API real y su verificador OIDC exponen el histograma HTTP cerrado de
OTel0.70.0/SDK1.46.0/exporter0.67.0/Prometheus client1.24.1. El middleware previo
se mueve a core con package/nombre exportado y gofmt fijado; no se promueve el
principal fijo del ejecutable histórico. Métricas requieren activación explícita
y puerto loopback. Next corre bajo el mismo supervisor native ya admitido.

Ensayo actual: PostgreSQL real, identidad RSA fixture, creación/replay y rechazos
401/400. Un fallo acotado de tabla causa HTTP500 reales. Prometheus3.14.0 conserva
la regla de ventana5m/umbral1%/hold10m/evaluación30s y llega a firing tras
600.234segundos en pending. Se restaura el dato;
1000replays con concurrencia8 mantienen un único pedido. Carga observada:
15.751s, p95 197.309ms; no es SLO productivo.
La alerta se recupera. Reiniciar Prometheus conserva la consulta histórica fija;
recuperar el controlador conserva el pedido; Go0/NextSIGTERM143/wrapper0 y árbol
nativo vacío. Todos los procesos propietarios terminan.

La retención Prometheus1h/64MB regula bloques, no una cuota dura de disco ni una
prueba de expiración durante este ensayo corto. La captura native sí se limita
a2MiB,12procesos/1GiB/1200s. El Collector separado V372 conserva exactamente sus
13archivos y binarios:1MiB/2backups/1día,2000observaciones/reinicio. Se documenta
esa correspondencia sin atribuirle logs del API actual. Las alertas de outbox y
backup del template no se presentan como señales conectadas por este delta.

Cuatro perfiles reconstruidos exactos, tres composiciones Go compiladas y
binario del host reconstruido byte-idéntico. Grafo seleccionado78módulos públicos
y4locales; consulta OSV actual0hallazgos. Tidy conserva el piso x/mod0.40.0 y los
checksums previos. Lint11archivos/34regiones revisadas,0sin triar/0nuevos blockers.
Fuentes oficiales y obligaciones legales preservadas; código de unión AUTHORED,
sin atribución empresarial. El lock distingue el historial del host nuevo.

T2809 queda cerrado para este host/ensayo local finito. Servicio permanente,
SCM/HA, routing/respondedor, cuotas target, SLI por servicio, PITR y despliegue
productivo permanecen CANDIDATE_TARGET explícitos; no se reducen a una credencial.
La documentación materializada contiene el comando y guía de recuperación.
T2801/T2810/TEST07/destino durable/notices ejecutables siguen abiertos.
ARCA penúltimo, dossier Daybreak/libxml2 último; no se investigaron.

FAIL943 RESOLVED_LOCAL. Primera resolución offline sin caché y preservación del
piso de seguridad registradas; no se repiten suites históricas sin delta.
Receipts/rutas/SHA y observaciones exactas: LOCAL_REFERENCE_OPERATIONS_V402.json.
