# V402 — referencia local firmada aceptada

Fuente330 y metadatos331, 4744archivos de artefacto, 325826657bytes. Dos builds independientes reales y ZIP determinista idéntico; SHA256 `dba7978d04d08dc2eedb112524708e220ba18cccf86c3a2142f8b1556ed1bf76`. OSV2.5.1: 454registros, cero vulnerabilidades; SPDX2.3 con437paquetes cubre335identidades. Firma Ed25519/OpenSSH e in-toto/SLSA comprobados con política confiable externa, sin copiar la clave privada al producto.

La extracción independiente verificó cada archivo, ruta, tamaño y hash. El runtime actual API+Next arrancó y recuperó el mismo pedido PostgreSQL de la prueba anterior: una fila durable. El binario API es idéntico al previamente probado con rollback manual, candidato fallido y recuperación; el lanzador es el mismo delta calificado. Los procesos se apagaron con árbol vacío. El token de fixture se renueva antes de cada petición; no se debilita su vencimiento.

Dos árboles de fuente externos e independientes coinciden en1653archivos. El compositor selecciona116packs;CI0.1.9 corrige sólo19etiquetas de procedencia, sin cambiar ningún byte de fuente330. NEW/EXISTING, preservación de archivos ajenos y rechazo de destino ocupado tienen recibos y tiempos anteriores conservados, sin repetirlos. Los insumos admitidos se preservaron byte por byte fuera de Temp. No se redistribuye pnpm restringido ni credenciales.

Destinos: `Elite Franchise Reference V402/reference-staging`, `artifact-v402-r330`, `signed-release-v402-r330`, `source-reference330-a`, `source-reference330-b`. Los paths absolutos, recibos y hashes están en LIBRARY_SIGNED_REFERENCE_ACCEPTANCE_V402.json y LIBRARY_EXACT_REFERENCE_V402.json.

Alcance: LIBRARY_INFRASTRUCTURE / LOCAL_FIXTURES. Sin cobros, mensajes, documentos reales, cuentas de usuario ni despliegue productivo. Esta evidencia de producto firmado es distinta del archivo portable de la biblioteca; su aceptación distributiva se registra en el expediente local excluido de ese archivo para evitar autorreferencia de hashes.
