# All Implementation Packs Materialization — 2026-08-27 V27

## Governing delta

This record succeeds V26. Historical records remain snapshots of their own source state.

The exact Microsoft/Azure sample `data-extraction-using-azure-content-understanding` remains official, MIT licensed and at the head of its active `main` branch, but its first deep adversarial rebuild does not admit any new immediate-use component. `OFFICIAL-UPSTREAM-ACQUISITION-CORE` advances from 0.4.22 to 0.4.23 so the rejection is visible in the immutable source lock and both Microsoft/document-intelligence profiles before acquisition. All thirteen dependent composition plans now request 0.4.23.

Source identity, archive bytes, commit and license hash remain unchanged. Classification changes from a generic `SAMPLE_ONLY` to `SAMPLE_ONLY_REJECTED_FOR_IMMEDIATE_ADOPTION`; this is a safer admission decision, not a source modification.

## Exact Azure evidence

`AZURE_CONTENT_UNDERSTANDING_DATA_EXTRACTION_REBUILD_2026-08-27_V1.md` records:

- declared requirements fail test collection because `parameterized` is omitted;
- audit-only addition `parameterized==0.9.0` yields 166 PASS and 19 warnings on Python 3.12.13;
- no integral Python/Terraform lock and no GitHub Actions workflow;
- a current 133-package freeze with 17 OSV records across three affected package-versions;
- anonymous registered Function routes and raw upload without size/type/malware/quarantine gates;
- a probe proving that confidence `0.0` is accepted into the persistence model;
- a probe proving that a failed lock wait still reaches upsert;
- preview REST `2025-05-01-preview` while current Microsoft Learn documents GA `2025-11-01`;
- disabled monitoring registration, Python 3.12/3.11 divergence, public network defaults, broad roles and a moving `main` pipe-to-shell deploy command.

`UP-FAIL-070` through `UP-FAIL-073` preserve those conditions. Local harness failures through LIB-FAIL-217 remain retained rather than erased, including the recurrence of incorrect `$LASTEXITCODE` handling and the initially mis-targeted classification hunk. The latter now has a permanent semantic regression asserting that Meta WhatsApp remains `SAMPLE_ONLY` while the Azure data-extraction source alone carries this new rejection.

## Executed pack gates

The rebuilt acquisition 0.4.23 pack materialized 18 files. Its direct suites returned:

```text
UPSTREAM_ACQUISITION_TEST_PASS negatives=5
UPSTREAM_LOCK_VALID sources=69 selected=69
SOURCE_PROFILE_TEST_PASS valid=9 negatives=5 positives=1
```

The Microsoft profile still selects twelve exact sources and the document-intelligence profile thirty. Selection is acquisition, not production admission; both profiles now expose the Azure rejection in `production_blockers`.

The structural verifier before adding this governing record returned:

```text
VERIFY_LIBRARY_PASS
packs=41 materialized_files=364 markdown_files=256
20/20 composition profiles PASS
```

The executable audit independently rebuilt the current sources and returned:

```text
VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit
packs=41
upstream_sources=69
document_sdk_artifacts=5
business_central_artifacts=1
provider_adapters=7
```

After this record, the structural verifier is repeated against 257 Markdown files and all audit temporaries are removed.

## Honest state

The official GA Content Understanding SDK remains separately fixed and usable only after project selection, access and gates. The Azure sample application is not called production code, not copied into an immediate-use runtime and not used to authorize file persistence. No local correction is attributed to Microsoft.

Global state remains `NOT_READY_UNDER_EXPANDED_USER_STANDARD`. This delta deliberately reduces future agent time by turning a promising but unsafe sample into an explicit fail-closed decision with reproducible proof.
