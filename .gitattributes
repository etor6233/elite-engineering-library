# Hash-locked library: preserve the exact accepted bytes, including mixed newlines.
# Unsetting text disables index/checkout newline conversion even with core.autocrlf.
# It does not disable textual diffs or any admission/security gate.
# Authority: https://git-scm.com/docs/gitattributes (text: Unset).
* -text
*.png binary
*.jpg binary
*.zip binary
