# Current successor authority — V403 NEXT

This document resolves R1-MAP-01, R1-MAP-02, CONSIST-02 and CONSIST-03. It does not rewrite the protected historical files.

The original library gate remains READY_FOR_LIBRARY_USE for the exact V402/337 snapshot. This successor is IN_PROGRESS until its own complete task ledger and receipt pass. No inherited READY or visual acceptance.

## Which state to use

1. Selected composition and its source lock identify the revision. Load that revision's execution cursor and next_action; never use a historical paragraph or numeric suffix as a current gate.
2. Frozen R1R2-20260914 inventories 212 packs. The original integral selected profile contains116packs/1653unique targets, the V403 extension120packs/1789targets before overlays. These are different counts: available vs selected vs effective files. Original AGENTS106/1418 is historical and must not determine current completeness.
3. The actual counts for NEXT are calculated from its final composed manifest. This document deliberately does not invent a total while owner deltas are still being connected.
4. HISTORY-MODEL-TRAINING-PIPELINE0.1.0 exists; the V373 evidence and current pack govern its narrow synthetic opt-in SFT/evaluation/rollback claim. An old AI-map sentence describing contract-only work is superseded. This is not authorization to train private data or to promote a model.

## Composition formats

The executable Markdown compositor contract is `compositionVersion: "1.0"`, `secretVariables`, `packs` with path/packId/version/acknowledgeConditions/files/variables. Every file is CREATE, its payload uses an exact quadruple fence and SHA-256, including its final-newline policy. PATCH/DELETE/triple-fence examples in the early historical PACK_CONTRACT section are not accepted executable inputs.

Overlay replacement is a separate operation with exact before/after hashes and explicit ownership. It does not grant the original compositor PATCH/DELETE behavior. No payload is silently overwritten. Dormant transport addition, reference qualification fixtures, UI/public/admin and cloud client activation are separate named layers.

`FRANCHISE_CLOUD_EXECUTION_PLAN_V403.md` with `schemaVersion: "1.0.0"` is a standalone cloud descriptor. Do not feed it directly to the Markdown compositor. Select the executable integral extension plan and apply its connected cloud owner. The new manifest governs the combination with public/admin R3; do not weaken old R3 guards.

## Routing and limits

The operating protocol remains the existing `markdown_system/FRANCHISE_PROJECT_OPERATING_PROTOCOL.md`; bridge18/18 stays unchanged. A consuming project selects this successor explicitly through its existing PROJECT_PACK_PLAN and authority/source lock. Blueprint, state/events, failures and next_action remain in the consumer; library knowledge and corrected reusable owners remain here.

No alternative is silently installed or discarded. The48surfaces/21document classes/11functions keep their separate evidence dimensions. Galaxy event methods still wait for material admission; this never grants auth roles or prevents already admitted product tasks.

Default design system in this composition is V403 only. The repaired legacy TS-DESIGN-SYSTEM0.1.1 is a compatibility artifact, not a second active theme. New visual candidates require explicit human approval.

Sources of these corrections: frozen review PACK_INDEX_CORRECTED, PROFILE_COVERAGE, STRICT_PACK_TARGETS, R1_COMPOSITION_FINDING and the actual source-composition receipts under ../composition. Method authority remains the library's COMPOSITION_PROTOCOL and PUBLIC_CODE_ARCHITECTURE_ADMISSION_STANDARD.
