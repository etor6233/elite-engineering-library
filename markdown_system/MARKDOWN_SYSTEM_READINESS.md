# Markdown System Readiness

- [x] V295 local — composition root1.9.1 difiere ARCA sin socket/rutas fiscales; 18 escenarios de configuración y 16 checks de custodia Windows, rebuild3/3 y 69 bindings documentados. Guía única con fuentes oficiales y pendientes explícitos; no selección de target/IdP, no live ni cierre de franquicia. Evidencia: `reconstruction_evidence/CREDENTIAL_PREPARATION_V295.md`.

- [x] V285 — compositor 0.2.2 compatible con StrictMode, suite y rebuild 3/3 PASS; tres entradas locales 0,30–0,42 s, reentrada sin alterar 13 archivos y 70 tests readiness. Cuatro records locales y 48 clasificaciones, sin cerrar evidencia por rellenarlas. Readiness real BLOCKED/44; no porcentaje. Evidencia: `reconstruction_evidence/MAINTENANCE_ENTRY_AND_SCOPE_V285.md`.

- [x] V284 — bridge NEW/EXISTING corrige redirecciones, marcadores invertidos y rollback; 18 aserciones rojas iniciales, 23/23 verdes, suite anterior y copia limpia PASS. Controles AUTHORED, no nuevo código upstream ni promoción. T2801 sigue abierto. Evidencia: `reconstruction_evidence/AGENT_BRIDGE_BOUNDARY_RECOVERY_V284.md`.

- [x] V283 — PROJECT-START-READINESS-VALIDATOR 0.6.1 corrige rechazo de CLI/AUTOMATION sin exigir web ficticia; 70 tests y rebuild 8/8 PASS, auth/evidencia/versiones preservadas. Expediente de mantenimiento reconciliado parcialmente y reporte real BLOCKED/87; T2801 no cerrado. Evidencia: `reconstruction_evidence/READINESS_CLI_CONTRACT_REPAIR_V283.md`.

- [x] V282 — corregida exclusión portable de expedientes locales exactos de preparación, 32 checks de selectores y 62 regresiones del gate PASS. Baseline real de mantenimiento creado, ronda A generada, readiness BLOCKED sin aprobaciones inventadas. T2801 sigue pendiente de contenido/assurance, no de esta incompatibilidad de almacenamiento. Evidencia: `reconstruction_evidence/MAINTENANCE_READINESS_BOUNDARY_V282.md`.

- [x] V281 — auditoría focal de fuente oficial de logging: Go 1.26.7 y 1.26.8 íntegros por digest, 278 casos oficiales + 11 probes propios PASS por runtime, cero SKIP; error de toolchain incompleto corregido en staging. Resolver USE_CONDITIONED_PACK / ready=false; no cierra T2809, no cambia runtime/perfil y no sustituye pruebas de privacidad/operación del journey. Evidencia: `reconstruction_evidence/OFFICIAL_STRUCTURED_LOGGING_AUDIT_V281.md`.

- [ ] V280 — 21 cores citados fuera del perfil integral; equivalencias/conexiones pendientes. GO-OBSERVABILITY-CORE reabierto como CANDIDATE: cuatro contraejemplos fallan y los cinco tests históricos siguen pasando; composición bloqueada y 48 IDs asignados exactamente al roadmap. Fallos locales 385–387 abiertos, sin atribuirlos a upstream. Evidencia: `reconstruction_evidence/CONNECTED_COVERAGE_AUDIT_V280.md`.

- [ ] V279 — continuidad de mantenimiento: el estado y su historial estaban ausentes y las allowlists los rechazaban. Se repara la separación local/distribución, con 14 casos de selectores y 24 tests del validator; el expediente de readiness/assurance de mantenimiento continúa pendiente y no se confunde con V278 probado. Evidencia: `reconstruction_evidence/LIBRARY_CONTINUITY_REPAIR_V279.md`.

- [x] V278 — host de statuses con cancelación/backoff/reportes acotados; historial Go read-only → BFF → agenda → ayuda versionada. Opt-in en configuración existente, sin otra cola/tabla/dependencia. Rebuild 67/742 y 22 hashes idénticos, 43 Go/WhatsApp sin SKIP, 102 web PASS/1 SKIP (conectado ejecutado aparte), 42 Python, cuatro navegadores de agenda y cuatro de notificación, vet/build y home/CSP PASS. Regresión de role=status ambigua registrada y corregida; 24 tests del execution validator PASS con raíz autoridad real. Doce archivos nuevos AUTHORED; no nuevo código copiado upstream ni cierre de montaje/identidad/alertas/live/producción. Evidencia: `reconstruction_evidence/WHATSAPP_HOST_OPERATOR_HISTORY_V278.md`.

- [x] V277 — WhatsApp 0.11.0 y async workers 0.3.0 conectan claim scoped → router/observer → completion job+inbox+audit transaccionales. Retry acotado, terminal/último intento caído preservados, takeover y payload drift rechazan writes antiguos; replay sin observaciones duplicadas. Nueve tests nuevos repetidos, rebuild 67/730 con seis hashes idénticos, 34 Go/WhatsApp y siete cola/processors sin SKIP, 42 Python, vet/build PASS. Dos archivos nuevos AUTHORED; no nuevo upstream/dependencia/migración ni cierre de UI/live/redrive. Evidencia: `reconstruction_evidence/WHATSAPP_SCOPED_JOB_COMPLETION_V277.md`.

- [x] V276 — WhatsApp 0.10.0 añade routing de statuses retenidos hacia receipts autorizados, sin IDs de cita aportados por webhook. Reusa verifier/HMAC/fence/observer; 20 negativos sin observación, replay y fallo parcial de dos mensajes recuperado sin duplicados. Reconstrucción 67/728, seis hashes, 25 Go/PG sin SKIP, 42 Python, vet/build y 0052 down/up PASS. Cuatro nuevos archivos AUTHORED; no completa jobs/inbox ni cierra UI/live. Evidencia: `reconstruction_evidence/WHATSAPP_RETAINED_STATUS_ROUTING_V276.md`.

- [x] V275 — GO-RELIABLE-ASYNC-WORKERS 0.2.0 corrige cierres de jobs vencidos y takeover con worker ID repetido. Generación exacta, row lock previo a comprobar reloj y presupuesto por handler; cuatro fallos semánticos reproducidos, siete tests/once subcasos nuevos sin SKIP, vet/build y 4/4 hashes reconstruidos PASS. Mismo perfil 67/724, sin nueva dependencia/migración/cola. Código AUTHORED con autoridades AWS/PostgreSQL; no cierra el worker WhatsApp, publisher/outbox ni efectos externos. Evidencia: `reconstruction_evidence/JOB_CLAIM_GENERATION_AND_EXPIRY_V275.md`.

- [x] V274 — WhatsApp 0.9.0 añade receptor HTTP opt-in con verificador Meta-adaptado existente y receipt/job PostgreSQL compartidos. ACK perdido tras commit + cuatro replays dejan un solo evento/job; bytes releídos vuelven a verificarse y producen observed_delivered con routing explícito de fixture. 14 negativos HTTP, suite Go/PG del package sin SKIP, 42 Python, vet/build y 5/5 hashes PASS; perfil 67/724. Raw retention requiere expediente/acceso/cifrado/borrado demostrados en target. Worker automático/UI/live siguen abiertos; no nuevo código oficial Meta. Evidencia: `reconstruction_evidence/WHATSAPP_HTTP_DURABLE_INGRESS_V274.md`.

- [x] V273 — GO-PROVIDER-INTEGRATION-CORE 0.1.2 corrige recepción desde conexión disabled y replay con otro proveedor/type/payload. Red seis subcasos; green siete con desactivación concurrente, test PG anterior, dominio/HTTP focal, vet/build y reconstrucción 2/2 PASS. Reutiliza inbox/job sin nueva tabla/dependencia; no implementa aún endpoint Meta. Conserva siete SKIP de navegador de la invocación HTTP amplia, sin presentarlos como PASS. Evidencia: `reconstruction_evidence/PROVIDER_INBOX_ADMISSION_SCOPE_V273.md`.

- [x] V272 — WhatsApp 0.8.1 refuerza tests de scope cambiado entre verificaciones, cancelación durante INSERT bloqueado y replay tras renovar conexiones. Tres mutantes de scope detectados; código productivo restaurado idéntico, suite Go/PG completa del package sin SKIP, 42 Python, vet/build y 3/3 hashes reconstruidos PASS. Sin nuevos runtime, tablas, inbox o código upstream. No cierra ingress/worker, UI ni cuenta live. Evidencia: `reconstruction_evidence/WHATSAPP_INTERRUPTION_SCOPE_REGRESSION_V272.md`.

- [x] V271 — WhatsApp 0.8.0 conecta anchor leído del fence accepted → verificador Python aislado/hash-locked → observaciones PostgreSQL inmutables → GET autenticado/read-only con timestamp y ambigüedad explícita. Cuatro procesamientos concurrentes insertan tres eventos; un solo intento de envío. 42 Python, suite completa del package Go/PG, vet/build, 51 migraciones/0051 down-up y 10/10 hashes PASS. Perfil 67/722. Montaje ingress/worker, UI y cuenta/entrega live siguen abiertos. Evidencia: `reconstruction_evidence/WHATSAPP_DURABLE_STATUS_OBSERVER_V271.md`.

- [x] V270 — WhatsApp 0.7.0 correlaciona SEND_RECEIPT anclado con mensajes/destinatarios de webhooks firmados mediante el único parser existente. Deduplicación, orden por timestamp, ambigüedad explícita y snapshot atómico sin send/SQL. 41 Python, tres tests focales Go de proceso/contrato, vet/build y 5/5 hashes reconstruidos PASS. Perfil 67/718, sin nuevas dependencias/migraciones. Consumer durable/UI y entrega live siguen abiertos. Evidencia: `reconstruction_evidence/WHATSAPP_ANCHORED_STATUS_RECONCILIATION_V270.md`.

- [x] V269 — lector BFF compartido con presupuesto de bytes previo a decodificación, cancelación temprana y errores acotados. Red 11 FAIL; reconstrucción 70 web PASS/1 SKIP, typecheck/build, tres HTTP nativos (gzip, chunked, deadline) y 4 archivos+lock idénticos. Perfil 67/716; cero dependencias/migraciones nuevas. Corrección AUTHORED, no código atribuido a Microsoft. No cierra UI WhatsApp ni statuses live. Evidencia: `reconstruction_evidence/BFF_STREAM_RESPONSE_BUDGET_V269.md`.

- [x] V268 — WhatsApp 0.6.0 añade GET autenticado y aislado para resultado histórico local; approval vencida no oculta accepted, lease vencido no habilita reenvío y delivery queda explícitamente no observado. 11 estados/integridad, 13 scope/errores y POST→GET con SQL read-only/RS256/fence PASS; full Go/vet/build, 22 Python y 5/5 hashes. Perfil 67/715, sin migración/dependencia nueva. UI/worker y statuses reales siguen abiertos. Evidencia: `reconstruction_evidence/WHATSAPP_READ_ONLY_OUTCOME_V268.md`.

- [x] V267 — WhatsApp 0.5.0 conecta API explícita de confirmación a verifier RS256/JWKS, aprobación PostgreSQL y fence existente. 11 negativos de parser, 13 rechazos HTTP y tres recorridos con concurrencia/pérdida HTTP/pérdida provider PASS; proveedor sintético. Full Go/vet/build, 22 Python y 4/4 hashes reconstruidos; perfil 67/713 sin nueva migración/dependencia. UI/worker, cuenta real y statuses/reconciliación permanecen pendientes. Evidencia: `reconstruction_evidence/WHATSAPP_AUTHENTICATED_DISPATCH_V267.md`.

- [x] V266 — WhatsApp 0.4.0 añade aprobación PostgreSQL inmutable ligada a confirmación comprometida, contacto y consentimiento actuales; propósito/política server-side, key por evento y revalidación previa al send. 19 negativos, seis invalidaciones, ocho aprobaciones concurrentes y replay fenced con proveedor sintético PASS; 22 Python, full Go/vet/build, 50 migraciones/down-up y reconstrucción 7/7 idéntica. Perfil 67/711. Falta API/worker, cuenta real y statuses/reconciliación. Evidencia: `reconstruction_evidence/WHATSAPP_APPOINTMENT_APPROVAL_V266.md`.

- [x] V265 — WhatsApp 0.3.0 conecta el Sender Go al owner Python sin duplicar HTTP ni ledger. Approval exacta, runtime/adapter hashes, stdio acotado/sin secretos en CLI, receipt validado y resultados ambiguos sin reenvío. 22 tests Python, Go/PG con child sintético, full Go/vet/build y round-trip 6/6 PASS. Perfil 67/706, sin nueva dependencia/migración. Approval durable de turno/contacto y statuses live siguen pendientes. Evidencia: `reconstruction_evidence/WHATSAPP_GO_DURABLE_BRIDGE_V265.md`.

- [x] V264 — WhatsApp 0.2.0 rechaza webhooks firmados de otra WABA/teléfono y conserva scope/destinatario/estado/tiempo/keys en evidencia v2. Red 1 FAIL/8 PASS; green 17/17, reconstrucción 5/5 y composición 67/704. Sin nuevas dependencias ni mensajes externos. Es corrección ADAPTED, no un nuevo upstream Meta. La notificación de turno y reconciliación durable siguen pendientes. Evidencia: `reconstruction_evidence/WHATSAPP_SCOPED_WEBHOOK_V264.md`.

- [x] V263 — agenda por fecha, selección de recursos por nombre y versiones internas; 4 navegadores sobre sesión sintética/TLS local/Next/Go/PG confirman y conservan estado tras recarga. Tres recuperan respuesta perdida post-commit sin otro POST. Origen configurado, scoped/bounded read, 54 tests web/build/typecheck, Go/PG y round-trip 12/12 PASS. No acredita login/IdP/PKI reales ni notificación externa. Evidencia: `reconstruction_evidence/OPERATOR_AGENDA_BROWSER_POSTGRES_V263.md`.

- [x] V262 — confirmación cliente BFF→Go→PG con firma RS256/JWKS local: dos intentos, un confirmed/version 3, un audit de operador y un outbox; lectura sólo por cliente propietario. Errores protegidos 401/403/409 conservados y JSONP rechazado. 52 tests web, typecheck/build, Go test/vet/build, PG y round-trip 3/3 PASS. No cubre todavía formulario/login autenticado, agenda cómoda ni notificación. Evidencia: `reconstruction_evidence/APPOINTMENT_CONFIRMATION_BFF_POSTGRES_V262.md`.

- [x] integración V260 — captura pública navegador→BFF→Go→PostgreSQL demostrada en cuatro proyectos Microsoft Playwright; replay sin duplicados, conflicto y rechazos de consentimiento/clave, cuatro leads/consentimientos/outbox/receipts reales. Se corrige la pérdida de Problem Details 409→502 con red/green; 40 tests web, typecheck/build, full Go/vet/build, suites PostgreSQL y smoke 4+8 PASS. Cinco archivos canónicos reconstruidos idénticos; sin dependencias nuevas. El resto del journey (respuesta/reserva/cotización/pedido) sigue abierto. Evidencia: `reconstruction_evidence/PUBLIC_LEAD_BROWSER_POSTGRES_V260.md`.

- [x] corrección V259 — Questions outbound 0.1.1 valida int64 canónico, mantiene incertidumbre ante HTTP no demostrado y exige identidad exacta durante reconciliación. Cuatro regresiones nuevas, full Go, PostgreSQL 49 migraciones y round-trip 3/3 pasan. V258 conserva evidencia histórica; el porcentaje 85 % se retira por falta de método sustentado. Evidencia: `reconstruction_evidence/MERCADOLIBRE_OUTBOUND_HARDENING_V259.md`.

- [x] integración V258 — `GO-MERCADOLIBRE-QUESTION-OUTBOUND 0.1.0` añade tres archivos `AUTHORED` al mismo package/ledger outbound: approval durable ligada a tenant+DeliveryKey+question+SHA-256(text), `POST /answers` UTF-8/2.000, confirmación exacta por GET v4 y reconciliación GET-only. `GO-PG-OUTBOUND-DELIVERY-FENCE 0.2.0` distingue rechazo terminal con evidencia de ambigüedad y ambos impiden un segundo POST. Round-trip 3/3, composición 67/704 y full Go/vet pasan con Go oficial 1.26.7. La fuente oficial Mercado Libre actualizada 2026-01-15 gobierna el contrato; el código no se atribuye falsamente al proveedor. Cuenta/token/scopes/moderación/privacidad/quota/callback/carga/seguridad/aceptación reales siguen condicionados.

- [x] integración V257 — Mercado Libre Questions dejó de ser una frontera aislada: `GO-MERCADOLIBRE-MARKETPLACE-ADAPTER 0.2.0` materializa topic `questions`, GET/search v4 y reconciliación `UNANSWERED`; `GO-OMNICHANNEL-LEAD-INGRESS 0.2.0` normaliza seller/buyer/item/text/PII al mismo raw+candidato/rechazo+outbox durable con `pending_policy`. `BANNED`, owner divergente, fecha/buyer ambiguos y estados desconocidos fallan cerrado. Son implementaciones `AUTHORED/ADAPTED` contra el contrato HTTP oficial, no código falsamente atribuido a Mercado Libre. Materialización 10/10 y 12/12, focal Go/vet, Secure Ops 37/37 y 101 casos, root 159/1.392/689/51, franquicia 66/701 y Audit integral PASS. Cuentas, callback live, contacto/respuesta, privacidad, carga, seguridad y aceptación siguen condicionados.

- [x] aseguramiento V256 — `EXECUTION-VALIDATOR 1.3.0` eleva el engineering contract a 1.1.0 sin crear un segundo plan: toda implementación declara procedencia, tier no inferior al mayor riesgo, cuatro métodos públicos oficiales para high/critical, source locks cuando hay upstream y las nueve dimensiones de corrección, contratos, seguridad, fallos, rendimiento, operación, recovery, release y UX. El gate `evidence` exige `proven`, cero blockers, verificaciones passed/hash-bound y evidencia distinta del artefacto y release; negativos rechazan compile-only, dimensión critical omitida, adapted sin lock y reuse de evidencia. Round-trip 15/15, 24/24 tests, cuatro planes, root y Audit con Go oficial 1.26.7 pasan. NIST/NASA/Google/Microsoft gobiernan el método; los cambios son `AUTHORED`, no código falsamente atribuido. Catálogo SAST corregido: OpenGrep permanece fuera del perfil. Root vigente: 159/1.390/688/51; franquicia 65/689.

- [x] endurecimiento V255 — el perfil acelerador conserva 65 packs/689 archivos, incluye DevSkim adaptado y release firmado portable, y excluye OpenGrep mientras Cosign permanezca `REJECTED_VERIFIER_RUNTIME`. `VERIFY_LIBRARY.ps1` ejecuta un positivo y tres negativos sintéticos —reintroducción de OpenGrep, omisión de DevSkim y omisión del release— antes de componer; `AGENTS.md` exige explícitamente el gate de release sin Git/CI hospedado. El pack rechazado sigue auditable fuera del producto y no se oculta ni se borra.

- [x] reconstrucción V254 — `PORTABLE-SIGNED-RELEASE-EVIDENCE-GATE 0.1.0` materializa 10/10 archivos y cierra candidate release local sin Git/Actions/cloud: build hash-locked ejecutado dos veces, ZIP byte-idéntico, OSV 2.5.1 exacto/0, SPDX 2.3, in-toto/SLSA, firma Ed25519 y verify OpenSSH con Authenticode Microsoft. Fixture E2E SHA `c6a018…9fe3`; tamper rechazado; publicación atómica. Microsoft SBOM Tool 4.1.5/main, Cosign 3.1.3 e in-toto-golang 0.11.0 se rechazaron por SCA actual. El pack OpenGrep/GitLab V253 conserva evidencia pero su installer queda `REJECTED_VERIFIER_RUNTIME` y se excluye del perfil ejecutable. Root 159/1.390/687/51, franquicia 689/65 y Audit con Go oficial 1.26.7 pasan.

## Gate de independencia

- [x] V261 — captura→disponibilidad→solicitud de turno durable y recuperación post-commit pasan en cuatro proyectos Microsoft Playwright; junto a captura previa, 8/8 conectados. Se corrigen receipt sin validar, replay sin header e interacción prehidratación. Go/PG y 40 tests web/build/typecheck, smoke 4+8 y round-trip 6/6 pasan, sin dependencias/migraciones nuevas. No equivale a confirmación por operador ni venta. Evidencia: `reconstruction_evidence/PUBLIC_APPOINTMENT_RECOVERY_V261.md`.

Estado vigente V258-Mercado-Libre-Question-Outbound / V257-Mercado-Libre-Questions / V256-implementation-assurance / V255-profile-admission / V254-release-portable y capacidades anteriores: **160 packs / 1.395 archivos materializables / 51 perfiles de composición / 121 fuentes del lock central + repositorios oficiales fijados dentro de packs / 16 source profiles**. El perfil de franquicia selecciona 67 packs/704 archivos y conserva un solo runtime de producto. `EXECUTION-VALIDATOR 1.3.0` evita promover código local o externo por compilación/reputación; V254 mantiene release firmado portable. OpenGrep/GitLab queda bloqueado, preservado como evidencia y fuera del perfil por el SCA actual de Cosign; DevSkim adaptado y fuzz Go siguen disponibles con alcance condicionado. Esto no transforma cuentas/credenciales/corpus/reglas/IdP/cloud/aceptación de un proyecto real en PASS.

Memoria vigente post-V295: **2.148 fallos/lecciones de biblioteca + 209 condiciones upstream = 2.357 IDs únicos**, cero estados locales `OBSERVED`, `FIX_PROPOSED` o `BLOCKED` abiertos; `UP-FAIL-203`–`209` permanecen abiertos por los grafos oficiales vulnerables de RESTler, DevSkim, Microsoft SBOM Tool, Cosign e in-toto-golang y por las condiciones de ASH 3.7.1, sin ocultarse. El lane OpenGrep/GitLab conserva su evidencia, pero queda bloqueado y fuera del perfil ejecutable porque su verifier Cosign no pasa SCA actual. Inventario actual: 160 packs/1.434 archivos/728 Markdown/51 perfiles; el último Audit integrado conserva su alcance histórico y no se presenta como repetido por las pruebas focales posteriores. Cualquier proyecto productivo conserva además sus gates live, regulatorios, de seguridad, carga, recovery, despliegue y aceptación. V294 agrega aceptación cliente→pedido único/GET/ayuda en 12 fases navegador; no confirma stock/pago/entrega ni cierra T2802/T2804. FAIL-423 de teardown Firefox permanece en el ledger del proyecto; no se oculta por repeticiones verdes. Continuación V294: mismos pedidos→reserva serial única y payment created; cuatro continuaciones PASS, importe/estado/vigencia legibles y snapshot idéntico tras reinicio PostgreSQL. No cierre del portal operativo de stock/pago/entrega.

- [x] reconstrucción V253 — `GITLAB-OPENGREP-SIGNED-SAST-GATE 0.1.0` materializa 5/5 `AUTHORED`; adquiere OpenGrep 1.29.0 exacto, verifica SHA-256 y firma Cosign contra identidad/issuer oficiales, y selecciona 122 reglas exactas GitLab 2.9.3 (117 `WARNING|ERROR`). Excluye con evidencia GitLab Enterprise y Commons Clause. Hostile TypeScript/Go produce exactamente `eslint.detect-eval-with-expression`/`gosec.G204-1`, clean=0, JSON/SARIF coinciden y tamper de runtime se rechaza. Root 158/1.380/684/50 y Audit 158/121 pasan. AWS ASH 3.7.1 fue evaluado pero no promovido por commit unsigned, advisory dev y suite predeterminada no verde; el lane directo evita esa superficie sin fingir procedencia GitLab del runtime.

- [x] reconstrucción V252 — `MICROSOFT-DEVSKIM-ADAPTED-SAST-GATE 0.1.0` materializa 4/4 `AUTHORED`; adquiere el commit Microsoft firmado `a452aa5…`, valida tree y hash canónico de 268 archivos pese al reempaquetado permitido por GitHub, conserva MIT/notices, añade SharpCompress 0.48.0 como adaptación explícita, usa NuGet.org exclusivo sin warnings, pasa build, 300/300 tests, SCA=0 y fixtures `DS189424` TypeScript/`DS112852` Go/clean=0. Produce runtime, provenance, manifest y receipt no sobrescribible. No se atribuye como bytes Microsoft ni se presenta como SAST interprocedural completo.

- [x] reconstrucción V251 — `CAPABILITY-GAP-RESOLUTION-GATE 0.1.0` materializa 4/4 archivos `AUTHORED`, gobernados por NIST SSDF, Google SRE, NASA V&V y SLSA sin copiar ni atribuirse su código. Valida refs locales dentro del project root, bytes/SHA-256, fuente oficial, revisión/licencia/claim, G0–G8, frescura, agotamiento y receipt no sobrescribible; tres positivos y seis negativos pasan, incluido el rechazo de evidencia sólo remota no preservada. `RESEARCH_INCOMPLETE` falla y obliga a continuar; sólo `USE_REUSABLE_PACK` marca `implementation_ready=true`. Root 156/1.371/678/48 y Audit online 156/121 pasan con el nuevo gate=1; tras el endurecimiento final de evidencia local, la revisión exacta volvió a pasar suite focal, root y preflight integral sin repetir descargas upstream no afectadas.

- [x] rechazo V250 — Microsoft DevSkim release/NuGet continúan en 1.0.90; el commit actual firmado `a452aa5…` fue adquirido por archive exacto y reconstruido con SDK .NET 10.0.400. Su feed Azure respondió 401; NuGet.org explícito permitió restore/build/test, pero NU1902 y los `.deps.json` confirman `SharpCompress 0.40.0` afectado en library/CLI/tests. DevSkim queda `REJECTED_COMPONENT / UPSTREAM_OPEN`; no se fuerza un override local ni se lo atribuye a Microsoft. CodeQL soporta Go/JavaScript, pero GitHub Team privado requiere además licencia GitHub Code Security y no es baseline gratuito universal.

- [x] reconstrucción V249 — `GO-NATIVE-FUZZ-GATE 0.1.0` materializa 4/4 archivos `AUTHORED`, sin copiar ni atribuir source Go/Google. Sigue las autoridades oficiales de Go para `FuzzXxx`, corpus semilla, `go test -fuzz` y targets rápidos/deterministas. Root 155/1.367/674/47 y Audit 155/121 pasan; el Audit ejecutó baseline, semilla y fuzz real con Go 1.26.7, y dos negativos rechazaron perfil deshabilitado y traversal. Cada proyecto aún debe aportar invariantes/semillas reales; el PASS no sustituye SAST, ZAP/DAST, carga ni seguridad ofensiva del target.

- [x] rechazo V248 — Microsoft RESTler tag 9.3.1/commit firmado `eaacd927…` fue reconstruido con SDK .NET 8.0.424 oficial hash-locked: build release, restore locked, 70/70 tests F# y 86/86 tests Python pasan, incluido fuzz source-exact. OSV 2.5.1 devuelve 23 hallazgos/19 IDs únicos sobre el grafo; el commit firmado actual de `main` repite el resultado. RESTler queda `REJECTED_COMPONENT / UPSTREAM_OPEN`, sin backports falsamente atribuidos. V249 materializa separadamente la alternativa oficial de fuzz nativo Go; no afirma equivalencia funcional con RESTler ni SAST/DAST.

- [x] reconstrucción V247 — `MICROSOFT-KIOTA-OPENAPI-CLIENT-GATE 0.1.0` materializa 7/7 archivos `AUTHORED`; no incrusta ni atribuye código Microsoft. Adquiere Kiota 1.35.0 desde su release/commit firmado, verifica ZIP/ejecutable/versión, genera dos clientes Go independientes desde el fixture oficial hash-locked y demuestra seis archivos de código byte-idénticos con SHA portable `d6dcb409…`; `kiota-lock.json` se valida semánticamente porque su path de origen es contextual. Go 1.26.7 test/vet pasan, el grafo congelado tiene 14 paquetes y OSV 2.5.1 devuelve cero findings actuales. Dos negativos rechazan contrato alterado y destino ocupado. Root 154/1.363/670/46 y Audit 154/121 pasan; autenticación, reglas, provider live, carga, seguridad ofensiva y producción siguen siendo gates del proyecto.

- [x] reconstrucción V246 — `MICROSOFT-DURABLE-DOCUMENT-ORCHESTRATION 0.1.2` materializa 13/13; un bloque `ADAPTED` preserva el worker oficial Microsoft fijado y cierra de forma determinista async generators, default executor y event loop. Suite 13/13, pipeline focal 8/8, compileall y Audit con `ResourceWarning` como error pasan sin loops ni sockets abiertos. El `StatusCode.CANCELLED` seguido por `Worker shutdown completed` es el log de stop del SDK, no una fuga.

- [x] reconstrucción V245 — TikTok Lead materializa 11 archivos Python y 5 Go; el wheel oficial exacto conserva metadata 1.1.3, versión interna 1.2.1 y SHA-256 fijado. Frozen install/`pip check`, 16/16 Python, full Go test/vet/build, PostgreSQL 18.6 con 49 migraciones y 38/38 tests sin skips pasan. Franquicia compone 63/675 y el Audit gobierna 16 adapters. Cuenta/permisos/suscripción live, autenticidad del webhook, política de contacto y target productivo permanecen condicionados.

- [x] reconstrucción V241 — composición de franquicia 61/659 desde destino vacío; Google Ads 31.4.0 y TikTok Business SDK 1.1.3 fijados a wheels oficiales; Meta Lead incorporado al verifier de 15 adapters; Audit online PASS. Go 1.26.7, frontend Node 24.14.1/pnpm 11.19.0 (38 tests/19 páginas), PostgreSQL 18.6 (49 migraciones, backup+restore), ARCA regenerado, Playwright runtime 4/4 + target 8/8 y Lighthouse cinco corridas con mínimos 0.99/1/1/1 pasan. El expediente `FRANCHISE_CLEAN_COMPOSITION_AND_PROVIDER_FRESHNESS_2026-09-04_V241.md` conserva procedencia, fallos y no-claims.

- [x] reconstrucción V240 — `EXECUTION-VALIDATOR 1.2.0` materializa 15/15 archivos `AUTHORED` y no incrusta código upstream. Su lock fija GitHub Spec Kit 1.0.1, AWS AI-DLC 1.0.1, NASA, Google SRE y xAI por claims estrechos. Un proyecto `NEW` o `EXISTING` conserva baseline/delta, paridad exacta con `tasks.md`, referencias/evidencia SHA-256, próximo paso, contexto mínimo y checkpoints append-only. Round-trip limpio, compileall y 20/20 tests pasan; el Audit global ahora los ejecuta obligatoriamente y reporta `execution_state_controls=1`. `COMPLETE`/`PRODUCTION` continúan bloqueados sin release, validación integral, recovery, aceptación y admission del target.

- [x] reconstrucción V236 — runtime conversacional durable y composition root materializan 17/17 archivos; PostgreSQL 18.6 aplica 46/46 y 0046 down/up/focal; replay exacto evita una segunda inferencia/efecto, scopes se resuelven por contacto, errores inciertos quedan retryable y terminales son inmutables. Plan de franquicia 56 packs/612 archivos y auditorías raíz/ejecutable pasan. Meta webhook live, TikTok Lead exacto y producción siguen condicionados.

- [x] reconstrucción V167 — supply 0.11.0 materializa 66/66 y backend 29/358 desde Markdown. Seis archivos `ADAPTED` y dos `AUTHORED` fijan siete owners/tests BCApps y Microsoft Learn para UOM. PostgreSQL 18.6 limpio pasó 0001–0033, full Go/vet/build, down/up 0033 y focal posterior: base factor 1/precisión explícita, BOX 12, 2.5→30 EA, residual 0.1, factor 2.5, duplicado y mutación 55000 fail-closed. La primera global expuso cinco teardowns UOM y contaminación outbox; se corrigieron, se descartó la base y la repetición completa pasó. El gate raíz gobierna 94/1075/523/backend358. Packaging balance/breakbulk completo, calendarios, sales/service/production cross-dock, planning/CTP, WMS externo, target y producción siguen condicionados.
- [x] reconstrucción V168 — supply 0.12.0 materializa 72/72 y backend 29/364 desde Markdown. Cinco archivos `ADAPTED` y uno `AUTHORED` agregan composición exacta y breakbulk/gather explícito desde siete owners/tests BCApps fijados. PostgreSQL 18.6 limpio pasó 0001–0034, full Go/vet/build, down/up y focal: 24 EA→2 BOX, replay/divergencia, Take/Place/outbox, carrera con un ganador, composición falsa e implícita fail-closed y 0.025 KG válido. Un corpus separado probó upgrade 0033→0034→down→up sobre 12.345 KG. El gate raíz gobierna 94/1081/524/backend364. Breakbulk automático en actividades, calendarios, otras demandas, planning/CTP, WMS externo, target y producción siguen condicionados.
- [x] reconstrucción V169 — supply 0.13.0 materializa 75/75 y backend 29/367 desde Markdown. Tres archivos `ADAPTED` y nueve owners actualizados conectan la composición V168 con recepción→put-away gobernada por los mismos siete owners/tests BCApps fijados. PostgreSQL 18.6 limpio pasó 0001–0035, full Go/vet/build, down/up, corpus legacy 0034→0035→down→up y focal: dos BOX preservados en destinos, reservas física/composición, registro conservando bultos, ruptura 6+6 rechazada sin autorización, ruptura autorizada con Take/Place/outbox/receipt binding, cancelación que libera ambas reservas y conversión posterior. El gate raíz gobierna 94/1084/backend367 y Audit gobierna 94 packs/121 fuentes. Picking/reposición conscientes del empaque, otras demandas, planning/CTP, WMS externo, target y producción siguen condicionados.
- [x] reconstrucción V171 — supply 0.14.0 materializa 78/78 y backend 29/370 desde Markdown. Tres archivos `ADAPTED` nuevos y doce owners actualizados trasladan la preferencia target-UOM y el `AllowBreakbulk` explícito de los owners/tests BCApps fijados a pick y reposición. PostgreSQL 18.6 limpio pasó 0001–0036, suite completa, full Go/vet/build y 0036 down/up: BOX→BOX se conserva; BOX→EA sin autorización revierte; autorizado reserva base+BOX, bloquea conversión concurrente, cancela liberando ambas capas o registra Take/Place/source activity, remanente y destino exactos; reposición repite la misma conservación hacia min/max. Gate raíz 94/1087/528/backend370 y Audit 94/121 pasan. Producción, otras demandas, calendarios, WMS externo, target y aceptación siguen condicionados.
- [x] reconstrucción V172 — supply 0.15.0 materializa 83/83 y backend 29/375 desde Markdown. Cuatro bloques `ADAPTED`, un expediente `AUTHORED` y diez owners actualizados fijan las fuentes exactas Microsoft de SalesLine/source-document/warehouse-pick y conectan pedido/línea colocados con binding inmutable variante→item/UOM, cantidad base, pendiente, replay exacto/divergente y pick packaging-aware. PostgreSQL 18.6 limpio pasó 0001–0037, suite completa, full Go/vet/build y 0037 down/up: draft/mismatch/exceso fallan, cancelación libera, registro consume pendiente y dos picks simultáneos por el remanente producen un ganador. Gate raíz 94/1092/529/backend375 y Audit 94/121 pasan. Shipment/facturación, service/production, target y aceptación siguen condicionados.
- [x] reconstrucción V173 — supply 0.16.0 materializa 88/88 y backend 29/380 desde Markdown. Cuatro bloques `ADAPTED`, un expediente `AUTHORED` y ocho owners actualizados fijan SalesPost, SalesShipmentHeader/Line, SalesWhsePostShipment y ERMSalesOrder exactos de Microsoft BCApps. PostgreSQL 18.6 limpio pasó 0001–0038, 24 pruebas SQL, 0038 down/up y full Go; pick registrado→shipment parcial/final conserva stock, composición UOM, reserva y FIFO cost con replay/divergencia y un ganador concurrente. La regresión transfer consume también BOX no-base. Gate raíz 94/1097/530/backend380 y Audit 94 packs/121 fuentes pasan; carrier, entrega, fiscal/pago y serial/específico siguen condicionados.
- [x] reconstrucción V223 — `GO-APPLICANT-ONBOARDING-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Alta segura anti-fake (franquiciado o cliente online): submitted→contact_verified→approved→activated|rejected con deduplicación de email/teléfono, velocidad por tenant, doble control (revisor ≠ solicitante) y activación sólo desde aprobado+verificado. Go 1.26.7: 8/8 tests PASS; `go test ./...` de 43 paquetes PASS; `go vet` exit 0; round-trip materializado PASS. El KYC documental/biométrico real queda como adapter CONDITIONED (proveedor externo).
- [x] reconstrucción V222 — `GO-OTP-VERIFICATION-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Verificación de email/teléfono por código de un solo uso: hash SHA-256 (nunca en claro), TTL, intentos acotados con lock, un solo uso. Go 1.26.7: 5/5 tests PASS; `go test ./...` de 43 paquetes PASS; `go vet` exit 0; round-trip materializado PASS.
- [x] reconstrucción V221 — `GO-RESILIENCE-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Resiliencia: circuit breaker closed→open→half-open (sonda única tras el timeout), apertura tras N fallos consecutivos, retry budget acotado con backoff inyectable. Go 1.26.7: 6/6 tests PASS; `go test ./...` de 41 paquetes PASS; `go vet` exit 0; round-trip materializado PASS.
- [x] reconstrucción V220 — `GO-SLO-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. SLO/error budget: disponibilidad, presupuesto restante, burn rate y alerta multi-ventana multi-burn-rate (Google SRE). Go 1.26.7: 6/6 tests PASS; `go test ./...` de 41 paquetes PASS; `go vet` exit 0; round-trip materializado PASS.
- [x] reconstrucción V219 — `GO-OBSERVABILITY-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Observabilidad: logging estructurado con redacción de PII/secretos (claves sensibles + valores tipo PAN) y contadores/histogramas con percentiles. Go 1.26.7: 5/5 tests PASS; `go test ./...` de 41 paquetes PASS; `go vet` exit 0; round-trip materializado PASS. La exportación OTLP/Prometheus es composición del runtime.
- [x] reconstrucción V218 — `GO-RATE-LIMIT-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Rate limiting/anti-abuse: token bucket tenant-scoped, deny fail-closed, refill a tasa fija y burst acotado. Go 1.26.7: 6/6 tests PASS; `go test ./...` de 41 paquetes PASS; `go vet` exit 0; round-trip materializado PASS.
- [x] reconstrucción V217 — `GO-FX-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Conversión multi-moneda: tasas inyectadas en runtime, deny-by-default (par sin tasa → error), monedas ISO 3 letras; sin tasas hardcodeadas. Go 1.26.7: 5/5 tests PASS; `go test ./...` de 37 paquetes PASS; `go vet` exit 0; round-trip materializado PASS.
- [x] reconstrucción V216 — `GO-PAYROLL-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Cálculo de nómina config-driven (deducciones fijas/porcentuales inyectadas por run), net fail-closed (nunca negativo) e idempotente; sin tablas impositivas hardcodeadas. Go 1.26.7: 5/5 tests PASS; `go test ./...` de 37 paquetes PASS; `go vet` exit 0; round-trip materializado PASS.
- [x] reconstrucción V215 — `GO-SEO-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. SEO: sitemap deduplicado (URLs absolutas http/https), reglas robots.txt, JSON-LD LocalBusiness y validación de longitud title≤60/description≤160 (Google Search Central). Go 1.26.7: 6/6 tests PASS; `go test ./...` de 37 paquetes PASS; `go vet` exit 0; round-trip materializado PASS.
- [x] reconstrucción V214 — `GO-WARRANTY-CLAIMS-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Garantías/reclamos: ciclo open→in_review→approved/rejected→resolved/closed fail-closed, con nota obligatoria al rechazar/resolver. Go 1.26.7: 4/4 tests PASS; `go test ./...` de 37 paquetes PASS; `go vet` exit 0; round-trip materializado PASS.
- [x] reconstrucción V213 — `GO-WAITLIST-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Lista de espera: join con posición por orden de llegada (sólo `pending`) y máquina pending→notified→seated/cancelled fail-closed. Go 1.26.7: 6/6 tests PASS; `go test ./...` de 37 paquetes PASS; `go vet` exit 0; round-trip materializado PASS.
- [x] reconstrucción V212 — `SUSTAINABILITY-PILLAR-GUIDANCE 0.1.0` materializa 1/1 archivo `AUTHORED` (Markdown) desde Markdown. Checklist del pilar Sustainability de AWS Well-Architected (principios, 6 áreas de mejores prácticas y checklist operativo) mapeado a controles existentes (FinOps V190, serverless, outbox, residencia V211). Round-trip materializado 1/1 PASS. El cálculo de kgCO₂e real requiere factores de emisión por región y telemetría del proveedor (runtime).
- [x] reconstrucción V211 — `GO-DATA-RESIDENCY-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Residencia de datos: registro de política por categoría (regiones permitidas), deny-by-default y fail-closed; categoría sin política o región fuera del conjunto se rechazan. Go 1.26.7: 6/6 tests PASS; `go test ./...` de 32 paquetes PASS; `go vet` exit 0; round-trip materializado PASS. La colocación física real (replicación/respaldo) es composición del proyecto.
- [x] reconstrucción V210 — `SOC2-TSC-CONTROL-MATRIX 0.1.0` materializa 1/1 archivo `AUTHORED` (Markdown) desde Markdown. Mapeo de controles SOC 2 (AICPA TSC 2017): categorías Security/A/PI/C/P y series Common Criteria CC1–CC9 a los controles existentes de la biblioteca (acceso, operaciones, cambios, doble control, mitigación de riesgos), con plantilla de evidencia por criterio. Round-trip materializado 1/1 PASS. La opinión Type II (periodo de observación + auditor independiente) es proceso externo, no código.
- [x] reconstrucción V209 — `GO-GDPR-CONSENT-ERASURE-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Gobierno GDPR: consentimiento específico/informado (finalidad + versión de política) y revocable (Art. 6/7), y máquina de borrado requested→in_progress→completed|rejected con retención legal que bloquea el borrado (Art. 17(3)) y base legal obligatoria al rechazar. Go 1.26.7: 7/7 tests PASS; `go test ./...` de 31 paquetes PASS; `go vet` exit 0; round-trip materializado PASS. La cascada real de borrado en los almacenes de dominio y la DPIA siguen como composición/proceso.
- [x] reconstrucción V208 — `GO-PCI-DSS-SCOPE-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Gobierno de alcance del CDE (PCI DSS 4.0): registro tenant-scoped de componentes, fail-closed ante datos de cuenta fuera de alcance, almacenamiento de SAD prohibido tras la autorización (Req 3.2) y SAQ obligatorio en alcance (Req 12.5.2); `Audit` re-verifica sin confiar en `Declare`. Go 1.26.7: 7/7 tests PASS; `go test ./...` de 30 paquetes PASS; `go vet` exit 0; round-trip materializado PASS. La autoevaluación SAQ/ASV/QSA anual sigue siendo proceso externo, no código.
- [x] reconstrucción V207 — `GO-DASHBOARDS-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Snapshot de KPIs por franquicia (ventas, pedidos, puntos, reseñas/NPS, citas) con agregación tenant-scoped y fail-closed; el render visual (gráficos) es composición del frontend. Go 1.26.7: 3/3 tests PASS; `go test ./...` de 29 paquetes PASS; `go vet` exit 0; round-trip materializado PASS.
- [x] reconstrucción V206 — `GO-ONBOARDING-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Onboarding del franquiciado: checklist ordenado de pasos deduplicados, progreso y completado idempotente por franquiciado (tenant-scoped). Go 1.26.7: 5/5 tests PASS; `go vet` exit 0; round-trip materializado PASS.
- [x] reconstrucción V205 — `GO-SOCIAL-POSTING-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Posteo a redes (Instagram/Facebook/TikTok/LinkedIn) con agenda futura y máquina de estados scheduled→posted/failed fail-closed; el envío real delega en los adapters de canal (V187/V189). Go 1.26.7: 5/5 tests PASS; `go vet` exit 0; round-trip materializado PASS.
- [x] reconstrucción V204 — `GO-I18N-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Catálogo de traducciones con cadena de fallback (locale→base→default→key) y pluralización CLDR one/other. Go 1.26.7: 4/4 tests PASS; `go vet` exit 0; round-trip materializado PASS.
- [x] reconstrucción V203 — `GO-HELP-CENTER-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Knowledge base por franquicia: artículos con categorías, concurrencia optimista por versión y ciclo draft→published→archived; la búsqueda delega en `GO-SEARCH-CORE`. Go 1.26.7: 5/5 tests PASS; `go vet` exit 0; round-trip materializado PASS.
- [x] reconstrucción V202 — `GO-POS-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Venta de punto de venta: líneas, total, tendido y vuelto, fail-closed (tendido corto rechazado, idempotente, tenant-scoped). Go 1.26.7: 4/4 tests PASS; `go vet` exit 0; round-trip materializado PASS.
- [x] reconstrucción V201 — `GO-MARKETING-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Campañas de marketing: canal, mensaje y lista de destinatarios deduplicada, agenda, idempotencia por destinatario y completado. Go 1.26.7: 6/6 tests PASS; `go vet` exit 0; round-trip materializado PASS.
- [x] reconstrucción V200 — `GO-SURVEYS-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Encuestas/NPS: score 0-10, una respuesta por cliente por encuesta y fórmula NPS estándar (promotores 9-10, detractores 0-6). Go 1.26.7: 5/5 tests PASS; `go vet` exit 0; round-trip materializado PASS.
- [x] reconstrucción V199 — `GO-REMINDERS-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Recordatorios de turnos: agenda futura, canales sms/email/push, `Due()` para despachar y `MarkSent` exactamente una vez. Go 1.26.7: 4/4 tests PASS; `go vet` exit 0; round-trip materializado PASS.
- [x] reconstrucción V198 — `GO-GIFT-CARDS-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Gift cards / crédito de tienda: emisión y canje con idempotencia por id y saldo fail-closed (nunca negativo). Go 1.26.7: 5/5 tests PASS; `go vet` exit 0; round-trip materializado PASS.
- [x] reconstrucción V197 — `GO-REVIEWS-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Reseñas con moderación: rating 1-5, una por autor por sujeto, ciclo pending→approved/rejected y promedio sólo sobre aprobadas. Go 1.26.7: 5/5 tests PASS; `go vet` exit 0; round-trip materializado PASS.
- [x] reconstrucción V237 — `GO-PG-CONTACT-CHANNEL-IDENTITY 0.1.0` materializa 8/8 archivos `AUTHORED`. HMAC tenant/canal evita identidad externa en claro; binding a lead, subject y policy/evidencia usa CAS, replay, ledger append-only y revocación. PostgreSQL 18.6 limpio pasó 47 migraciones y focal 0047; aislamiento, actualización, revocación y carrera pasan dos veces. El E2E nuevo usa resolver PostgreSQL real hasta cotización y replay; 49 paquetes, vet/build, round-trip y root 149/1311/656/44 + perfil 57/620 pasan. Propiedad de cuenta, método live de verificación, reglas jurídicas y producción siguen condicionados.
- [x] reconstrucción V238 — `GO-PG-OUTBOUND-DELIVERY-FENCE 0.1.0` materializa 11/11 archivos `AUTHORED`. Claim antes del proveedor, request hash, receipt HMAC/evidencia, replay, lease/timeout `unknown`, reconciliación explícita y ledger append-only impiden un segundo envío automático. `NewProduction` rechaza canales no durables. PostgreSQL 18.6 limpio pasó 48 migraciones/focal; E2E completo mantiene una llamada sender; 50 paquetes, vet/build y round-trip pasan. Exactly-once provider no se afirma; receipt/status/live siguen condicionados.
- [x] reconstrucción V239 — `GO-META-LEAD-WEBHOOK-SIGNAL 0.1.0` materializa 9/9 archivos (2 `ADAPTED`, 7 `AUTHORED`) gobernados por `fbsamples/messenger-platform-samples@354ee221` y `fbsamples/lead-ads-webhook-sample@c0843165`. Challenge, HMAC SHA-256 raw-body, batches, PostgreSQL antes del ACK, replay/divergencia/outbox y append-only pasan; base limpia 49 migraciones, dos suites focales y full 51 paquetes/vet/build pasan. Root 151/1331/660/44, perfil 59/640 y Audit 151/121 pasan; sólo los gates Meta live permanecen separados.
- [x] reconstrucción V196 — `GO-REFERRALS-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Referidos: códigos únicos, sin auto-referido, un referido por referente y máquina pending→converted→rewarded fail-closed. Go 1.26.7: 5/5 tests PASS; `go vet` exit 0; round-trip materializado PASS.
- [x] reconstrucción V195 — `GO-PROMOTIONS-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Cupones: descuento percent (basis points) o fijo (minor units), con vencimiento, mínimo de orden, límite de usos y canje fail-closed. Go 1.26.7: 6/6 tests PASS; `go vet` exit 0; round-trip materializado PASS.
- [x] reconstrucción V194 — `GO-LOYALTY-CORE 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Ledger de puntos tenant-scoped: earn/burn con entradas idempotentes, historial inmutable y saldo fail-closed (nunca negativo, nunca sobre-canjear). Go 1.26.7: 6/6 tests PASS; `go vet` exit 0; round-trip materializado PASS.
- [x] reconstrucción V193 — `GO-QR-CORE 0.1.0` materializa 3/3 archivos `AUTHORED` (stdlib-only) desde Markdown. Identidad QR segura: payload versionado, tenant-scoped y checksum SHA-256 tamper-evident, resolución contra el dominio fail-closed (cross-tenant y manipulación rechazados). Go 1.26.7: 5/5 tests PASS; `go test ./...` de 15 paquetes PASS; `go vet` exit 0; round-trip materializado PASS. Imagen (`skip2/go-qrcode` MIT) y `Resolver` real contra producto/pedido/cliente siguen como composición del proyecto.
- [x] reconstrucción V192 — `TS-DESIGN-SYSTEM 0.1.0` materializa 5/5 archivos `AUTHORED` (React/TS) desde Markdown. Design system accesible: tokens de color con contraste WCAG AA **verificado por Node** (12/12 pares ≥4.5:1; los fallos iniciales verde/ámbar fueron corregidos), tipografía/spacing/radios/breakpoints, y primitivos UI mobile-first (Button/Card/Input/StatusBadge/Empty/Loading/Error) con tokens como única fuente. `tokens.test.ts`: 3 aserciones. Gate `pnpm typecheck/test/build` + Playwright visual siguen pendientes (entorno).
- [x] cierre posterior de la reauditoría V191 — la revisión 0.1.0 fue degradada correctamente porque sólo conservaba punteros; V236 la reemplazó por `GO-APP-WIRING 0.2.0` + `GO-CONNECTED-CONVERSATION-RUNTIME 0.1.0` y demostró canal→presupuesto→Responses→aprobación→dominio→PostgreSQL→respuesta/replay, sin segunda inferencia ni segundo efecto. Permanece `CONDITIONED` sólo por cuentas, adapters, privacidad, carga, seguridad, recovery y aceptación del target.
- [x] reconstrucción V190 — `GO-FINOPS-CORE 0.1.0` materializa 8/8 archivos `AUTHORED` (5 Go stdlib-only + 3 SQL) desde Markdown. Gobierno de costo de nube (`COST-FINOPS`): tags `tenant`/`environment`/`owner` obligatorios, inventario único con dedup, audit que detecta untagged/drift/leak, budget por tenant fail-closed y teardown que contabiliza cada recurso. Go 1.26.7: 7/7 tests PASS; `go test ./...` de 13 paquetes PASS; `go vet` exit 0. PostgreSQL 18.6 real (initdb→up→test→down): `ON_ERROR_STOP=1` exit 0 (untagged y over-cap rechazados) y down limpio. Scan real del provider y perfil serverless (Cloud Run) siguen pendientes.
- [x] reconstrucción V189 — `GO-SMS-CHANNEL-ADAPTER 0.1.0` materializa 2/2 archivos `AUTHORED` (stdlib-only) desde Markdown. Adapter SMS (Twilio-compatible) con basic auth y body form-encoded; credenciales del entorno. Go 1.26.7: 8/8 tests `httptest` PASS en `msgchannels`; `go test ./...` de 12 paquetes PASS; `go vet` exit 0; round-trip materializado PASS. Completa los canales (Meta Graph + email + SMS); cuenta real y gate pnpm siguen pendientes.
- [x] reconstrucción V188 — `GO-AGENT-DOMAIN-BINDING 0.1.0` materializa 3/3 archivos `AUTHORED` (stdlib-only) desde Markdown. Binding del chatbot a los endpoints reales (cita, cotización, estado, devolución vía rechazo de entrega) con mapeos de negocio por `Config` y fail-closed ante servicio/producto desconocido o lead ausente. Go 1.26.7: 6/6 tests con `httptest` PASS; `go test ./...` de 12 paquetes PASS; `go vet` exit 0; round-trip materializado PASS. Mapeos reales, canal SMS y gate pnpm siguen pendientes.
- [x] reconstrucción V187 — `GO-MESSAGING-CHANNEL-ADAPTERS 0.1.0` materializa 4/4 archivos `AUTHORED` (stdlib-only) desde Markdown. Adapters de canales: Meta Graph (WhatsApp/IG/FB) con verificación HMAC-SHA256 de webhook y envío de texto, y email B2B vía API HTTP (SendGrid-compatible); credenciales del entorno. Go 1.26.7: 6/6 tests con `httptest` PASS; `go test ./...` de 11 paquetes PASS; `go vet` exit 0; round-trip materializado PASS. Cuenta Meta/relay, canal SMS y binding del `Domain` siguen pendientes.
- [x] reconstrucción V186 — `GO-LLM-OPENAI-ADAPTER 0.1.0` materializa 3/3 archivos `AUTHORED` (stdlib-only) desde Markdown. Adapter que implementa `aifoundation.LLMProvider` sobre Chat Completions OpenAI-compatible; API key del entorno (Bearer), modelo exacto, `response_format: json_object` con schema, fail-closed en status/contenido vacío. Go 1.26.7: 4/4 tests con `httptest` PASS; `go test ./...` de 10 paquetes PASS; `go vet` exit 0; round-trip materializado PASS. Cuenta/provider real y tiering se inyectan en runtime; canales y binding del `Domain` siguen pendientes.
- [x] reconstrucción V185 — `TS-MULTIROLE-ONBOARDING 0.1.0` materializa 6/6 archivos `AUTHORED` (Next.js/React) desde Markdown. Panel multi-rol simplificado (dueño/administrador/empleado/cliente) + onboarding por rol sobre el BFF web: visibilidad por permiso (`visibleSections`), dashboard de espacio de trabajo y guías de uso, gateados por sesión OIDC. Estado `RECONSTRUCTIBLE` honesto: round-trip SHA-256 PASS, gate `pnpm typecheck/test/build` pendiente por falta de `node_modules`/pnpm offline en este host. El rol nunca es atajo de autorización.
- [x] reconstrucción V184 — `GO-CHANNELS-CORE 0.1.0` materializa 4/4 archivos `AUTHORED` (stdlib-only) desde Markdown. Abstracción de canal: contrato `Channel` (receive/send), registro por código y dispatcher que rutea el mensaje entrante al responder y devuelve la respuesta por el mismo canal, sin ampliar tenant ni canal. Go 1.26.7: 4/4 tests PASS; `go test ./...` de 9 paquetes PASS; `go vet` exit 0; round-trip materializado PASS. Adapters de SDK reales (Meta Graph/Twilio/SMTP) y frontend multi-rol siguen pendientes.
- [x] reconstrucción V183 — `GO-CONVERSATION-TOOLS 0.1.0` materializa 5/5 archivos `AUTHORED` (stdlib-only) desde Markdown. Contrato `Domain` agnóstico (citas, cotización/venta, estado, devolución) + tools que bindean cada intención del agente a ese contrato, con parser determinista `clave: valor` y `ErrNeedsInfo` fail-closed. Go 1.26.7: 6/6 tests PASS; `go test ./...` de 8 paquetes PASS; `go vet` exit 0; round-trip materializado PASS. Implementación real del `Domain` (endpoints Go), conversión texto→estructura por LLM, canales y frontend multi-rol siguen pendientes.
- [x] reconstrucción V182 — `GO-HUMAN-APPROVAL-CORE 0.1.0` materializa 6/6 archivos `AUTHORED` (3 Go stdlib-only + 3 SQL) desde Markdown. Cola de aprobación humana anti-estafa: separación de deberes (revisor ≠ solicitante), doble control para montos altos, velocidad por sujeto, dinero (pago/devolución) nunca auto-aprobado y decisiones inmutables. Go 1.26.7: 7/7 tests PASS; `go test ./...` de 7 paquetes PASS; `go vet` exit 0. PostgreSQL 18.6 real (initdb→up→test→down): `ON_ERROR_STOP=1` exit 0 y down limpio. Ejecución del efecto aprobado en workers de dominio, identidad real de revisores y UI de revisión humana siguen pendientes.
- [x] reconstrucción V181 — `GO-LLM-ECONOMY-CORE 0.1.0` materializa 6/6 archivos `AUTHORED` (stdlib-only) desde Markdown. Capa de gobierno de costo casi nulo: cache semántica (`$0` en hit, reutiliza `search.CosineSimilarity`), compresión de prompt con presupuesto de tokens, ledger de gasto por tenant fail-closed y gobernador cache→llm→blocked. Go 1.26.7: 5/5 tests PASS; `go test ./...` de 6 paquetes PASS; `go vet` exit 0; round-trip materializado PASS. Tokenizer/adapter de LLM real y tiering por proveedor siguen `CONDITIONED`.
- [x] acelerador V180 — `FRANCHISE_ACCELERATOR.md` (fast-lane en 7 pasos, cobertura de las 48 superficies con clasificación `REQUIRED`/`OPTIONAL`/`NONE_WITH_REASON` para una franquicia de alto nivel, inyección de una capability en proyecto existente —ej. chatbot—, y lista fail-closed de inputs) + `markdown_system/FRANCHISE_COMPLETE_PACK_PLAN.md` (39 packs únicos con `pack_id`/`pack_version` exactos: backend 29 + IA/datos 5 + web 5 adicionales; el pack común de licencias se cuenta una vez). Compositor 0.2.1 y su suite pasaron; la composición limpia produjo 510 archivos, Go 1.26.7 pasó test/vet/build y web pasó instalación frozen, typecheck, 38 tests y build. Cableado en `AGENTS.md`. Decisión de plataforma: mobile = PWA recomendado (web responsive existente), desktop = `NONE_WITH_REASON`, embedded/IoT = `CONDITIONED` (solo flota electromovilidad). PostgreSQL, navegador, proveedores y producción se prueban por proyecto y no se heredan de este PASS.
- [x] reconstrucción V179 — `GO-DATA-ANALYTICS-CORE 0.1.0` materializa 7/7 archivos `AUTHORED` (4 Go stdlib-only + 3 SQL) desde Markdown. Cierra las superficies `DATA-INGEST` y `ANALYTICS-BI` a nivel de contrato: landing inmutable con replay idempotente por (source, external_id, payload sha), late-data bound por `MaxLateness` y ownership de fuente; definiciones de métrica versionadas (code+version únicos) con freshness fail-closed y reconciliación. Go 1.26.7: 5/5 tests PASS; `go test ./...` de 5 paquetes PASS; `go vet` exit 0. PostgreSQL 18.6 real (initdb→up→test→down): `ON_ERROR_STOP=1` exit 0 (replay dedup `INSERT 0 0`, rechazo received-before-event, métrica+snapshot) y down limpio. Warehouse/lakehouse y motor de agregación real siguen `SPEC_ONLY`.
- [x] reconstrucción V178 — `GO-CONVERSATIONAL-AGENT 0.1.0` materializa 9/9 archivos `AUTHORED` (stdlib-only) desde Markdown. Núcleo determinista del agente conversacional 24/7 (superficie `RAG-AGENTS`): ruteo de intención acotado (`KeywordRouter`), autorización de tools por intención (`ToolRegistry`), recuperación de conocimiento tenant-scoped (`SearchKnowledge` sobre `internal/search`), guardrails fail-closed (`aifoundation.SafetyGate`) y handoff humano con evidencia append-only. Go 1.26.7: 13/13 tests PASS; `go test ./...` de los 4 paquetes (aifoundation+search+cache+agent) PASS en árbol de trabajo y en árbol materializado (round-trip); `go vet` exit 0. LLM provider real, canal (web/WhatsApp), tools de dominio (agenda→journey, venta→commerce, devoluciones→return workers) y evals siguen pendientes.
- [x] reconstrucción V177 — `GO-CACHE-CORE 0.1.0` materializa 6/6 archivos `AUTHORED` (stdlib-only) desde Markdown. Borde de cache gobernado: keys tenant-scoped (`Scope.Key` niega tenant vacío), store en memoria TTL+LRU con presupuesto de bytes, evicción LRU y carga cache-aside single-flight que evita stampede y no envenena con errores; valores copiados en lectura. Go 1.26.7: 11/11 tests PASS en árbol de trabajo y en árbol materializado (round-trip), `go vet` exit 0. Sin migración ni dependencias. Adapter Redis (RESP) y RAG-AGENTS (consumidor) siguen pendientes.
- [x] reconstrucción V176 — `GO-SEARCH-CORE 0.1.0` materializa 11/11 archivos `AUTHORED` (8 Go stdlib-only + 3 SQL) desde Markdown. Índice de búsqueda gobernado por tenant: full-text PostgreSQL (`tsvector`/GIN + `websearch_to_tsquery` + `ts_rank` con config `simple` y peso title A/body B), filtros `kind`/`facets`, `embedding real[]` y búsqueda vectorial por coseno; negación por defecto de tenant. Go 1.26.7: 11/11 tests PASS en árbol de trabajo y en árbol materializado (round-trip), `go vet` exit 0. PostgreSQL 18.6 real (initdb→up→test→down): `ON_ERROR_STOP=1` exit 0 con 5 aserciones (full-text+ranking, aislamiento tenant/org, facets, round-trip embedding) y down limpio. Migración `0040` (up/down). Adapter pgx, modelo de embeddings, Faiss/DiskANN (pinnados, no materializados), pipeline de indexación y consumidores RAG/CACHE siguen pendientes.
- [x] reconstrucción V175 — `GO-ML-AI-FOUNDATION 0.1.0` materializa 10/10 archivos `AUTHORED` (stdlib-only, sin SDK ni proveedor) desde Markdown. Define el borde `LLMProvider` y la cadena de gobierno IA: pin exacto de revisión (digest sha256, `latest`/`auto` rechazados), registro de versiones con un único `active` y auditoría inmutable, salida validada contra schema objeto cerrado (`additionalProperties:false`), gates de seguridad fail-closed y promoción gated por evaluaciones. Go 1.26.7 windows/amd64: 27/27 tests PASS en árbol de trabajo y en árbol materializado (round-trip), `go vet`/`go build` exit 0; `-race` no ejecutado por falta de cgo. Sin migraciones ni dependencias; se compone bajo `internal/aifoundation/` en el módulo `elite.local/enterprise`. Adapter de proveedor real, clasificador de contenido, recognizer de PII, SEARCH, CACHE y el agente conversacional siguen pendientes.
- [x] reconstrucción V174 — fulfillment 0.4.0 materializa 15/15 y backend 29/386 desde Markdown. Cinco bloques `ADAPTED`, un expediente `AUTHORED` y cuatro owners actualizados fijan autoridad exacta Microsoft BCApps/Amazon sin presentar Go local como código upstream. PostgreSQL 18.6 limpio pasó 0001–0039, 25 pruebas SQL, 0039 down/up, full Go/vet/build y composición. Customer shipment→carrier report concurrente→entrega técnica conserva replay/evidencia/versiones, corrige referencias provider nulas y crea cero handovers automáticos. Gate raíz gobierna 94/1103/531/backend386 y Audit gobierna 94 packs/121 fuentes. Cuenta/webhook/polling, custodia, serial/específico, checklist/aceptación y producción target siguen condicionados.
- [x] control conectado V170 — readiness 0.6.0 materializa 8/8 con igualdad de paths/SHA y pasa 62/62. Cada capability `REQUIRED` debe estar cubierta por un journey que conecte persona/rol, interfaz, autorización, API, dominio, dato/efecto, auditoría/observabilidad, respuesta/error, ayuda, capacitación, soporte, actualización, E2E/negativos y rollback/recovery bajo una misma versión; referencias desconocidas, cobertura aislada o drift fallan cerrado. Audit gobierna 94 packs/121 fuentes. Es control ejecutable `AUTHORED`, no funcionalidad de producto ni evidencia target.

- [x] entrada común para Codex y Claude Code;
- [x] bridge portable sin Git — `INSTALL_AGENT_BRIDGE.ps1` conserva instrucciones preexistentes, instala Skills progresivas para Codex y Claude, usa rutas relativas, limita `AGENTS.md` a 32 KiB, es idempotente y falla atómicamente ante conflicto; regresión vacía/existente/vendorizada/Codex-only/Claude-only/negativa y `quick_validate.py` oficial PASS; aprobación de import externo Claude sigue siendo autoridad del usuario;
- [x] asesoramiento ejecutable V137 — readiness 0.5.0 materializa 8/8; catálogo oficial-source-routed A–H, términos/porqué/inputs/ejemplo no asumido/verificación/NO_APLICA, prompts atómicos y byte-exactos, referencias obligatorias por ronda, 54/54 y `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit`; no inventa respuestas ni convierte intake en evidencia productiva;
- [x] contrato de blueprint;
- [x] contrato de implementation pack;
- [x] protocolo de composición;
- [x] compositor real con perfiles backend/web, detección de colisiones, lineage y hashes finales;
- [x] catálogo honesto de brechas;
- [x] foundation portable `REBUILD_VERIFIED / CONDITIONED` — schema + semántica equivalente Go/Python; faltan rulesets legales por producto;
- [x] datos baseline — 23 tablas, repositories verticales, queries, workers, migraciones y restore lógico pasaron PostgreSQL 18.6; roles/RLS, PITR/failover y workload son gates del target;
- [x] identidad/autorización baseline y contrato productivo — OIDC Go, permisos y organizaciones por recurso, portal PKCE/state/nonce, sesión JWE server-only y negativos SQL/HTTP pasaron; OpenID Foundation 5.2.4 + Playwright 1.62.1 exactos alimentan un adapter que exige issuer/app/roles/tenants reales, conformance, efectos positivos, negativos sin filtración/mutación, matriz cross-tenant, sesiones y break-glass. Sus 12/12 regresiones sintéticas no sustituyen la ejecución contra el IdP/target;
- [x] frontend/portales baseline — catálogo, leads y portales admin/cliente/fábrica reconstruyen; journeys OIDC→Go→PostgreSQL pasaron con aislamiento por organización/subject;
- [x] módulos empresariales baseline integrada — catálogo/CRM, compras/fábrica/stock, comercio/precio/pago, logística, service/recall, franquicias/comunicaciones, queries y provider edge componen en un ejecutable;
- [x] integración/observabilidad/deploy baseline `RECONSTRUCTIBLE` — webhooks/inbox/jobs/mapping/reconciliation, OTel/SLO, CI/rollback/backup y packaging; provider/cloud concretos siguen siendo selección del blueprint;
- [x] 112 packs/1206 archivos materializan desde Markdown; perfiles vigentes conservan 42 composiciones y 16 source profiles/121 fuentes. Readiness 1/8, email 14/142, persistence 5/56, backend 29/386, web 6/84, Amazon Shipping 2/48, Easy Ship 2/34, delivery evidence 2/33, Supply Sources 2/34, MLI 3/43 y External inventory 2/34;
- [x] adquisición sin Git de 121 upstreams oficiales fijados por commit/archive SHA-256; incluye dieciséis perfiles hash-linked y el perfil empresarial de 22 fuentes con Kubernetes/kubectl 1.37.0, OpenID Foundation 5.2.4, ZAP 2.17.0, PostgreSQL 18.6, k6 2.2.0, Cloudflare, Playwright, Spec Kit y las autoridades anteriores. Commits unsigned/firmas válidas/licencias/disclaimers/condiciones permanecen explícitos; candidatos inseguros se rechazan. Prestigio nunca sustituye contrato, licencia, hashes ni gates;
- [x] artefactos runtime documentales fijados — catorce wheels/sdists oficiales Microsoft/Google/OpenAI/Oracle con runner reconstruido; wheel+source estable para Azure Document Intelligence 1.0.2, Azure Content Understanding 1.1.0, Google Document AI 3.15.0, Google Cloud Storage 3.13.1, OpenAI 3.3.1 y Oracle OCI 2.185.0, más Toolbox condicionado. OCI fue importado en Python 3.12 con cliente AI Document y siete interfaces públicas, `pip check` y SCA completa 23/0/0-skips; los samples Azure DI/CU desde bytes, `prebuilt-invoice`, `prebuilt-documentSearch` y analyzer copy materializan source/test/licencia byte-verbatim con contratos offline, sin autorizar exactitud ni persistencia y sin extender cobertura a proforma, packing list, BOL o aduana;
- [x] equivalencia funcional histórica de la vertical V4 para sus 64 archivos y gates declarados;
- [x] reconstrucción web BFF V140 vigente, sin golden/DB TypeScript: perfil 6/84 con jornada, entrega, excepción, receipt/disposition y solicitudes downstream explícitas; frozen offline, typecheck, 38 tests, build y licencias PASS. Microsoft Playwright runtime 4/4 + target 8/8 y Lighthouse cinco runs permanecen gates independientes. Consumers/reconciliación real, listados completos, policy/firma, red y edge/IdP/backend/a11y AT/RUM/load/seguridad/deploy/producción permanecen condicionados;
- [x] licencias/notices reproducibles — Go report/check/save real; pnpm prod report real; gate con hashes y rechazo de expresiones no revisadas. La aprobación legal del artefacto seleccionado nunca se preautoriza;
- [x] `golden_starter/` y workspaces temporales eliminados después de reconstrucción V4.
- [x] materializador, updater, compositor y verificador fail-closed cubren manifests, hashes, metadata/secciones, fences anidados, rutas/IDs únicos, selección, colisiones, residuos y perfiles;
- [x] memoria de fallos persistente — contrato y plantilla por proyecto; el estado vigente está publicado al inicio de este documento. Los snapshots históricos permanecen en este documento y el ledger conserva cada causa, corrección y prueba de no recurrencia; las referencias rechazadas nunca se presentan como implementación inmediata;
- [x] lifecycle de dependencias y autocorrección — contratos/templates obligatorios para inventario, EOL/advisories, candidates aislados, locks/SBOM/notices, baseline↔candidate, canary/rollback y supersession de información obsoleta; routers/readiness/verificador los exigen;
- [x] maintenance scanner oficial — Google OSV-Scanner 2.5.1 source/binario/checksum/provenance fijados; perfil web reconstruido detectó 150 packages y el runtime lock AWS Durable Execution ocho paquetes, ambos con cero matches conocidos en sus consultas fechadas, sin convertirlo en garantía ni habilitar `fix` automático;
- [x] monitoring baseline sin gasto incremental — perfil/record obligatorios combinan Dependabot incluido con OSV oficial local y Windows Task Scheduler; prohíben scheduled OSV Actions, add-ons y cloud pago sin autorización, verifican binario/reporte/exit codes/freshness y mantienen producción bloqueada hasta probar runtime monitoring;
- [x] respuesta a vulnerabilidad upstream — reabre admisión y bloquea promotion; exige identidad/consumers/reachability y fuentes oficiales; separa release corregida, patch oficial no publicado, ausencia de fix, supply-chain comprometida y falso positivo demostrado; todo backport conserva provenance local y todo resultado actualiza incidentes, SBOM, locks y lecciones;
- [x] `VERIFY_EXECUTABLE_LIBRARY.ps1` reconstruye todos los packs, prueba el source lock y expone modos `Preflight`/`Foundation`; el preflight actual falla cerrado si faltan toolchains o si las cuentas/corpus/entornos del proyecto no están probados;
- [x] auditoría ejecutable V77 — `VERIFY_LIBRARY_PASS` + `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit` sobre 56 packs/531 archivos/382 Markdown contando V77; el estado posterior contiene 383 Markdown por la reauditoría AWS GenAI IDP, sin cambiar packs. Se mantienen 110 fuentes oficiales, catorce perfiles source y 32 perfiles de composición. Amazon Textractor materializa 12/7 oficiales/4 contratos, Queries 2/1, Linux 69/16 y SCA 15/0+26/0; el perfil AWS compone 7/75 y el auditor reconoce `aws_textractor_official_components=1`. AWS Powertools materializa 8/5 verbatim/4 contratos; AWS Durable Execution materializa 12/8 verbatim/4 contratos, SCA fechada 8/0 y el perfil conjunto compone 3/43. Sus gates cloud/empresariales siguen condicionados;
- [x] auditoría ejecutable V78 — `VERIFY_LIBRARY_PASS` sobre 57 packs/536 archivos/387 Markdown y 32 perfiles, más `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit` sobre el mismo código antes de añadir su evidencia; Azure compone 8/73 y el auditor ejecuta por separado Azure DI `official_invoice_samples=1` y Azure CU `official_azure_cu_samples=1`. El source lock 0.4.63 conserva 110 fuentes/14 perfiles y rechaza explícitamente Google classify/split, HITL deprecated y splitter histórico sin perder los samples estrechos admitidos;
- [x] auditoría ejecutable V79 — `VERIFY_LIBRARY_PASS` sobre 58 packs/542 archivos/389 Markdown contando V79 y `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit` sobre el mismo código antes de su evidencia; Azure compone 9/79 y el auditor prueba separadamente `official_invoice_samples=1`, `official_azure_cu_samples=1` y `official_azure_cu_copy_samples=1`. El copy sample conserva source/test Microsoft exactos pero permanece bloqueado para autoejecución porque crea, actualiza y elimina recursos;
- [x] auditoría ejecutable V80 — `VERIFY_LIBRARY_PASS` antes de evidencia sobre 59 packs/548 archivos/390 Markdown y `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit`; Azure compone 10/85 y añade `official_azure_cu_binary_samples=1` con source/test Microsoft commit=sdist, 5 contratos y 3 syntax. El corpus final contiene 391 Markdown contando V80. AWS OCR queda rechazado por suite no portable, 633 lint findings y 31 IDs vulnerables; el validador PO Acme queda rechazado por no tener tests/lock y por reglas no estrictas/específicas;
- [x] auditoría ejecutable V81 — `VERIFY_LIBRARY_PASS` sobre 59 packs/548 archivos/391 Markdown antes de evidencia y `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit`; artifact core 0.3.0 conserva 6 archivos y eleva el lock a 12 artefactos oficiales. OCI 2.185.0 añade wheel+sdist exactos, siete interfaces AI Document importadas, `pip check` y SCA completa 23/0/0-skips; Azure sample integral, SAP archivado y Oracle DevRel invoice-only permanecen rechazados. El corpus contiene 392 Markdown contando V81;
- [x] auditoría ejecutable V82 — `VERIFY_LIBRARY_PASS` sobre 59 packs/548 archivos/392 Markdown antes de evidencia y `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit`; source pack 0.4.64 conserva 23 archivos y sube a 111 fuentes/14 profiles. AWS BDA PO añade archive firmado MIT-0, schema/notebook y 15 PDF+ground-truth/75 ítems consistentes; permanece `PINNED_CANDIDATE` por cero tests/lock, IAM amplio, schema abierto y cloud/costo. El corpus contiene 393 Markdown contando V82;
- [x] auditoría ejecutable V83 — `VERIFY_LIBRARY_PASS` sobre 59 packs/551 archivos/393 Markdown antes de evidencia y `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit`; Google official process pack 0.2.0 materializa 13 archivos, incluidos `handle_response_sample.py` y su test exactos del commit Google firmado `dc0eecc…`. El código oficial entrega `DocumentSchema` por `ProcessOptions.schema_override`; hashes/sintaxis y contrato con SDK 3.15.0 PASS. El perfil mínimo compone 2/19 y la lane Google completa 8/81. El corpus contiene 394 Markdown contando V83;
- [x] auditoría ejecutable V84 — `VERIFY_LIBRARY_PASS` sobre 59 packs/551 archivos/395 Markdown y `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit`; routing 0.3.0 conserva las 21 clases base, admite 0..N clases adicionales bajo el mismo contrato y emite receipt V3 con baseline/custom IDs. Materialización 4 archivos y 11/11 tests PASS; el positivo incorpora `letter-of-credit-amendment` sin cambiar código y el negativo rechaza traversal. Routing compone 2/27, durable conserva 5/51 y Google 8/81;
- [x] auditoría ejecutable V85 — `VERIFY_LIBRARY_PASS` sobre 60 packs/568 archivos/397 Markdown antes de evidencia y `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit`; Google lifecycle 0.1.0 materializa 17 archivos, incluidos doce source/tests exactos del commit firmado `dc0eecc…` y el bloque oficial dataset/migración atribuido. Trece Python compilan, verificador static/full PASS, cinco tests Google offline-safe PASS y portabilidad Windows pasó con path de 255 caracteres. El perfil lifecycle compone 3/36, Google completa 9/98 y el corpus contiene 398 Markdown contando V85;
- [x] auditoría ejecutable V86 — `VERIFY_LIBRARY_PASS` sobre 60 packs/572 archivos/398 Markdown antes de evidencia y `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit`; artifact core 0.3.1 fija 14 artefactos y runtime Google 0.1.3 instala 23 wheels hash-locked con Document AI 3.15.0 + Storage 3.13.1, `pip check`, imports y Google OSV 23/0. Lifecycle 0.2.0 materializa 21 archivos, incluidos 16 source/tests Google exactos; 17 Python compilan, verifier static/full y cinco tests offline-safe PASS. Lifecycle compone 3/40, Google 9/102 y el corpus contiene 399 Markdown contando V86;
- [x] auditoría ejecutable V87 — `VERIFY_LIBRARY_PASS` sobre 61 packs/580 archivos/401 Markdown antes de evidencia y `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit`; Microsoft AVM SFTP 0.1.0 materializa 8 archivos —3 Microsoft `VERBATIM`—, fija Storage AVM 0.33.0 y Bicep 0.46.1 por hash, compila y rechaza password/`rcwdl`. El perfil compone 1/8 y el corpus contiene 402 Markdown contando V87; el deploy, la transferencia DEV y el retained-original separado siguen condicionados;
- [x] auditoría ejecutable V88 — `VERIFY_LIBRARY_PASS` sobre 62 packs/585 archivos/404 Markdown antes de evidencia y `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit`; secure email MIME core materializa 5 archivos `AUTHORED`, compila y pasa 18/18 pruebas; el perfil de procesamiento compone 5 packs/54 archivos con Powertools, Durable Execution y gate de seguridad. El corpus contiene 405 Markdown; infraestructura Mail Manager/SES y E2E AWS siguen abiertos;
- [x] auditoría ejecutable V90 — `VERIFY_LIBRARY_PASS` sobre 64 packs/603 archivos/408 Markdown antes de evidencia y `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit`; GuardDuty release materializa 9 archivos, 16/16 pruebas, cfn-lint 1.55.1 y build 2.191 PASS. El perfil email compone 5/52 y el corpus contiene 409 Markdown contando V90; cuenta AWS, costos, SES identity/MX, KMS/aislamiento, plan/SNS, canaries, DLQ/redrive y rollback live siguen condicionados;
- [x] auditoría ejecutable V91 — `VERIFY_LIBRARY_PASS` sobre 65 packs/614 archivos/410 Markdown antes de evidencia y `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit`; dispatch GuardDuty→Magika→AWS IDP materializa 11 archivos, 14/14 pruebas, cfn-lint 1.55.1 y build hash-locked de 2.193 archivos de contenido con cero bytecode PASS. El perfil email compone 6/63 y el corpus contiene 411 Markdown contando V91; AWS/IDP/config/corpus/costos/canaries/idempotencia empresarial/recovery live siguen condicionados y storage automático permanece false;
- [x] auditoría ejecutable V92 — `VERIFY_LIBRARY_PASS` sobre 66 packs/622 archivos/413 Markdown y 35 perfiles, más `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit` sobre 66 packs/111 fuentes con `aws_idp_immutable_evaluation_handoffs=1`. El handoff AWS IDP materializa 8 archivos, ejecuta 14/14 pruebas, cfn-lint 1.55.1 y round-trip real `Document`/`load_hook_document` con `idp_common` 0.6.5/CPython 3.12.14 PASS; profile cerrado, hook `onError: fail`/no-mutation, HITL diferido, snapshot/dispatch inmutables, cero bytecode y business persistence false. El perfil email compone 7/71; AWS live, config/corpus/HITL/KMS/Object Lock/DLQ/restore/costos y consumidor idempotente continúan condicionados;
- [x] auditoría ejecutable V93 — `VERIFY_LIBRARY_PASS` sobre 66 packs/622 archivos/414 Markdown y 35 perfiles, más `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit` sobre 66 packs/111 fuentes. El handoff final 0.1.2 materializa 8/8, ejecuta 20/20 pruebas y cfn-lint 1.55.1 PASS; `Completed` exige tracking consistente/cobertura exacta/reviewer conocido/timestamp zonificado y emite evidencia Object-Locked autocontenida ligada a document/config/schema/ejecución con identidades hashadas; `Skipped` queda no revisado. El perfil email conserva 7/71 y business persistence false. AWS live, carrera upstream de review, config/corpus/políticas, KMS/Object Lock/DLQ/restore/costo y consumidor transaccional/idempotente siguen condicionados;
- [x] auditoría ejecutable V94 — `VERIFY_LIBRARY_PASS` sobre 67 packs/630 archivos/416 Markdown/35 perfiles y `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit` sobre 67 packs/111 fuentes con `aws_idp_evaluation_decision_workers=1`. El worker 0.1.1 materializa 8/8, ejecuta 13/13 y cfn-lint 1.55.1 PASS; profile/policy cerrados, evaluationId/payload validation/partial batch, prefijos tenant, referencias exact-version, schemas cerrados, review completa y decisión Object-Locked negativa ante schema inválido, sin datos ni business authority. Email compone 10/92. AWS live, policy/corpus/HITL target, carga/DLQ/restore/costo y persistencia negocio+outbox siguen condicionados;
- [x] auditoría ejecutable V95 — `VERIFY_LIBRARY_PASS` sobre 68 packs/641 archivos/419 Markdown/36 perfiles y `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit` sobre 68 packs/111 fuentes con decision worker y persistence boundary en uno. Worker 0.2.0 8/8+13 tests emite decision v2/snapshot ref; boundary 0.1.0 11/11+12 tests verifica evento/policy/snapshot y transacciona intake+outbox. cfn-lint/round-trip PASS; email 12/106, focal 3/22. AWS/Aurora live, policy/migration/role/RLS/races/DLQ/restore/rollback/costo y mappings finales siguen condicionados;
- [x] auditoría ejecutable V96 — `VERIFY_LIBRARY_PASS` sobre 69 packs/651 archivos/421 Markdown/36 perfiles y `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit` sobre 69 packs/111 fuentes con `debezium_postgres_outbox_runtimes=1`. Boundary 0.2.0 11/11+12 tests conserva decisión/policy/snapshot, outbox append-only y roles RLS/CDC; runtime Debezium 0.1.0 10/10+7 tests fija índice/manifests/configs OCI oficiales, preflight, registro fail-closed y contrato consumer. Email 13/116 y focal 4/32 PASS. Pull/launch, PostgreSQL logical replication, Kafka/broker, TLS/auth, consumer inbox+efecto atómico, restart/redelivery/order/load/recovery/rollback/costo y observabilidad live siguen condicionados;
- [x] auditoría ejecutable V97 — `VERIFY_LIBRARY_PASS` sobre 70 packs/673 archivos/423 Markdown/36 perfiles y `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit` sobre 70 packs/111 fuentes con `debezium_postgres_inbox_consumers=1`. Consumer 0.1.0 22/22 conserva cinco Java Red Hat exactos y adaptación declarada Quarkus 3.39.1/Jackson 2.22.1; build Java 21, PostgreSQL 18.6 real 10/10, 4×8 entregas concurrentes y SCA CycloneDX/OSV 154/0 PASS. Inbox+proyección se confirman sólo tras commit; replay igual converge y offset/evento divergentes se rechazan. Email 14/138 y focal 5/54 PASS. Kafka/TLS/auth/rebalance/crash/load/recovery/restore/observabilidad y mappings de dominio live siguen condicionados;
- [x] auditoría ejecutable V98 — `VERIFY_LIBRARY_PASS` sobre 71 packs/680 archivos/425 Markdown/36 perfiles y `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit` sobre 71 packs/111 fuentes con `google_cel_document_mappings=1`. Mapping 0.1.0 materializa 7/7 AUTHORED, compila Java 21/Maven 3.9.16 desde cwd ajeno, pasa 7/7, genera SBOM 18 y OSV 0; no copia source Google. CEL-Java 0.13.0 conserva 2.831/2.836 tests upstream y los cinco fallos Windows documentados. Email 15/145 y focal 6/61 PASS; schemas/corpus/aprobación/load/rollback/unión transaccional, Kafka y producción live siguen condicionados;
- [x] auditoría focal V99 — consumer 0.2.0 materializa 24/24 desde Markdown, pasa static, Java 21/Maven 3.9.16, PostgreSQL 18.6 18/18, cuatro carreras 1+7, rechazo cross-class y rollback de inbox+proyección ante clase/mapping inválidos, con SCA CycloneDX/OSV 170/0. CEL-Java 0.13.0 se ejecuta dentro de la misma transacción que inbox/proyección y ACK sólo ocurre después del commit. Email compone 14/140 y persistence focal 5/56; Kafka/TLS/auth/rebalance/crash/load/recovery/restore/observabilidad y mappings/schemas/corpus target siguen condicionados;
- [x] auditoría ejecutable V100 — readiness validator 0.2.0 materializa 4/4, pasa syntax y 26/26 unit/CLI. El JSON distribuido continúa bloqueado; documentos requeridos no pasan sin clases/variantes únicas y una clase no autoriza storage automático sin decisión READY, mapping ID/version/expression SHA, input/output schema ID+SHA, corpus/evaluación/aprobación y owner exactos. `VERIFY_LIBRARY_PASS` sobre 71/682/427 Markdown contando esta evidencia y `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit` cierran el corpus; respuestas, accesos y evidencia target nunca se inventan.
- [x] auditoría ejecutable V102 — readiness 0.4.0 materializa 5/5 y pasa 40/40. Cada clase automática conserva modo direct/outbox y exactamente los diez campos mapping del consumer; expresión+SHA, domain/class, IDs/version/schema/output keys y capabilities se validan antes de build, mientras review-only rechaza configuración latente. El renderer vuelve a validar, selecciona clase/variante exactas, rechaza template alterado/duplicados/overwrite y sólo copia mapping sin abrir el perfil. El auditor cruza dinámicamente readiness↔consumer y `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit` cierra 71 packs/111 fuentes; `VERIFY_LIBRARY_PASS` gobierna 71/683/429 Markdown contando V102.
- [x] auditoría ejecutable V89 — snapshot histórico: 63 packs/594 archivos, receiver 11/11, MIME 18/18 y perfil 4/43 PASS;
- [x] plataforma empresarial Microsoft ejecutable condicionada — BcContainerHelper 6.1.16 oficial adquirido por bytes/SHA-256/Gallery SHA-512 y licencia, import 309 comandos y resolución de artifact BC 28.4.53241.0 PASS; pack/plan 6 archivos y tests offline PASS; host real bloqueado por administrador/Windows containers/Docker/EULA/licencia y contenedor no creado;
- [x] distribución portable allowlisted incorpora hashes por entrada y checksum lateral del ZIP.

Estado técnico histórico de bootstrap: **READY_FOR_PROJECT_BOOTSTRAP**. Estado bajo el estándar ampliado solicitado por el propietario: **NOT_READY_UNDER_EXPANDED_USER_STANDARD** hasta cerrar perfiles ejecutables y evidencia para todas las capacidades declaradas, empezando por document intelligence multi-clase. La biblioteca permite que Codex/Claude parta de un blueprint y componga backend/web verificables, pero todavía no autoriza afirmar que el sistema integral o la extracción de documentos del negocio estén terminados.

Este estado se apoya en reconstrucciones independientes, PostgreSQL real, OIDC/HTTP E2E, hashes, builds, tests y evidencia de licencias; no en cantidad de documentos.
- [x] auditoría ejecutable V103 — Microsoft Playwright 1.62.1 oficial queda fijado por release/commit/archive/npm/licencia/browsers.json; pack 0.1.0 materializa 9/9, frozen offline, runtime Chromium desktop+móvil 2/2 y home web 2/2. El perfil web compone 4/53, pasa typecheck/14 tests/build; `VERIFY_LIBRARY_PASS` gobierna 72/692/431 Markdown y `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit` gobierna 72 packs/112 fuentes con `microsoft_playwright_browser_gates=1`. Roles/IdP/backend, Firefox/WebKit, a11y AT, performance, seguridad y producción siguen condicionados.
- [x] auditoría ejecutable V104 — Playwright 0.1.1 fija Chromium 1234, Firefox 1538 y WebKit 2336. Runtime 4/4 y home real 4/4 PASS; WebKit conserva CSP y adapta sólo transporte loopback promovido, sin bypass ni errores silenciados. El perfil continúa 4/53; `VERIFY_LIBRARY_PASS` gobierna 72/692/432 Markdown y el auditor ejecutable exige la matriz oficial. Roles/IdP/backend, a11y AT, performance, seguridad y producción siguen condicionados.
- [x] auditoría ejecutable V105 — Google Lighthouse 13.4.1 oficial queda fijado por release/commit/tree/archive/npm/licencia/integrity/attestations; pack 0.1.4 materializa 10 archivos y el perfil web compone 5/63. Frozen offline, cinco policy tests, cinco runs reales y cleanup PASS; Foundation conjunta pasa backend Go, web typecheck/14 tests/build, Playwright runtime 4/4+target 4/4 y Lighthouse performance mediana `0.97`, accessibility/best-practices/SEO `1.00`. `VERIFY_LIBRARY_PASS` gobierna 73/702/434 Markdown contando esta evidencia; `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit` gobierna 73 packs/113 fuentes y Foundation termina exit 0. Commit unsigned, roles/IdP/backend, AT/RUM/load, seguridad, PostgreSQL recovery, cloud/deploy/rollback y producción siguen condicionados.
- [x] auditoría ejecutable V106 — Google SafeValues 1.2.0 queda fijado por commit/tree/archive/licencia/manifest y npm tarball/integrity, con cero dependencias runtime; bridge 0.2.1 añade adapter y tres regresiones DOM-XSS. Perfil web 5/65 pasa frozen install, typecheck/17 tests/build, Playwright 4+4 y Lighthouse cinco runs con performance mediana `0.99`/resto `1.00`; acquisition core 0.4.68 valida 114 fuentes y perfil empresarial 15. CodeQL privado sin entitlement, DevSkim/Application Inspector con SharpCompress vulnerable y safety-web alpha se rechazan. `VERIFY_LIBRARY_PASS` gobierna 73/704/435 Markdown; Foundation y la auditoría final sobre 114/114 fuentes terminan exit 0. SafeValues no prueba seguridad universal: CSP/Trusted Types/sinks/raw HTML, roles/IdP/backend, AT/RUM/load/ofensiva, PostgreSQL recovery, cloud/deploy/rollback y producción siguen condicionados.
- [x] auditoría ejecutable V107 — Google CSP Evaluator 1.1.8 queda fijado por commit/tree/archive/licencia y npm tarball/integrity/signature; bridge 0.3.0 añade CSP estricta con nonce fresco según la guía oficial Next.js y cuatro regresiones, mientras Playwright 0.1.2 prueba nonce/home en cuatro perfiles. Perfil web 5/69 pasa frozen install, typecheck/21 tests/build dinámico, Playwright 4+8 y Lighthouse cinco runs con performance mediana `0.99`/resto `1.00`; acquisition core 0.4.69 valida 115 fuentes y perfil empresarial 16. Microsoft ESLint SDL sobre ESLint 9 EOL se rechaza y el evaluador conserva su disclaimer/no exhaustividad. `VERIFY_LIBRARY_PASS` gobierna 73/708 antes de esta evidencia; Audit y Foundation terminan exit 0. Edge/CDN/WAF, Trusted Types/sinks/raw HTML, roles/IdP/backend, AT/RUM/load/ofensiva, PostgreSQL recovery, cloud/deploy/rollback y producción siguen condicionados.
- [x] auditoría ejecutable V108 — acquisition core 0.4.72 fija k6 2.2.0, PostgreSQL 18.6 y OWASP ZAP 2.17.0 con identidades oficiales exactas; lock 118/perfil empresarial 19 PASS. Secure operations 0.6.0 materializa 22 archivos y 38 casos (37 PASS/1 skip de symlink): runner oficial más `LOAD_RESILIENCE`, `POSTGRES_RECOVERY` y `OFFENSIVE_SECURITY` semánticos. `VERIFY_LIBRARY_PASS` gobierna 73/724 y backend 16/111; Audit termina con 73 packs/118 fuentes y Foundation con Go 1.26.7 termina exit 0 tras backend, web, Playwright 4+8 y Lighthouse cinco runs. No existe target productivo real y quedan cinco adapters semánticos, por lo que `PROJECT_CONDITIONED` se conserva.
- [x] auditoría ejecutable V109 — acquisition core 0.4.73 fija cloudflare-go 7.9.0 por release/commit verificado/tree/archive/licencia/go.mod/rulesets; lock 119, perfil empresarial 20 y perfil Cloudflare 2 PASS. Secure operations 0.7.0 materializa 25 archivos y 47 casos (46 PASS/1 skip): añade `EDGE_CDN_WAF` semántico con DNS proxy, WAF managed/custom, TLS Full strict, bypass al origen y caché pública/privada hash-bound. `VERIFY_LIBRARY_PASS` gobierna 73/727 y backend 16/114; Audit termina con 73 packs/119 fuentes y Foundation con Go 1.26.7 termina exit 0 tras backend, 47 casos productivos, web, Playwright 4+8 y Lighthouse cinco runs. No existe zona/target productivo real y quedan cuatro adapters semánticos, por lo que `PROJECT_CONDITIONED` se conserva.
- [x] auditoría ejecutable V110 — acquisition core 0.4.74 fija OpenID Foundation Conformance Suite 5.2.4 por release GitLab/commit/tree/archive/licencia/pom/runner y conserva explícito el tag lightweight sobre commit unsigned; lock 120, quince perfiles, empresa 21 y perfil OpenID 2 PASS. Secure operations 0.8.0 materializa 28 archivos y ejecuta 59 casos (58 PASS/1 skip): `IDENTITY_AUTHORIZATION` exige conformance OIDC, roles con efectos, seis negativos, matriz cross-tenant completa, cinco pruebas de sesión y break-glass gobernado. `VERIFY_LIBRARY_PASS` gobierna 73/731 y backend 16/117; Audit termina con 73 packs/120 fuentes y Foundation con Go 1.26.7 termina exit 0 tras backend, 59 casos productivos, web, Playwright 4+8 y Lighthouse cinco runs. No se ejerció un IdP/target real y quedan tres adapters semánticos —providers, deploy/rollback y aceptación empresarial—, por lo que `PROJECT_CONDITIONED` se conserva.
- [x] auditoría ejecutable V111 — secure operations 0.9.0 materializa 31 archivos y ejecuta 73 casos (72 PASS/1 skip). `PROVIDERS` admite diez identidades explícitas y exige cuatro ejecuciones reales por cada selección: sandbox por operación, webhook autenticado/durable o N/A doblemente revisado, idempotencia/dedup con reconciliación y retry acotado ante throttling; términos, scopes, costo, regiones, retención y owners independientes quedan hash-bound. Stripe, Mercado Pago, Amazon, Google, Meta, TikTok y Firebase se atan a sus fuentes oficiales exactas; Mercado Libre conserva `AUTHORED` explícito sobre HTTP oficial. Round-trip 31/31 PASS. No se ejerció cuenta/operación externa real y quedan dos adapters semánticos —deploy/rollback y aceptación empresarial—, por lo que `PROJECT_CONDITIONED` se conserva.
- [x] auditoría ejecutable V112 — acquisition core 0.4.75 materializa 25 archivos y fija Kubernetes/kubectl 1.37.0: release/commit/tree/ZIP/licencia/changelog y checksum/runtime exactos, sin afirmar firma del tag/commit unsigned. Lock 121/121, 16 perfiles y enterprise 22 PASS. Secure operations 1.0.0 materializa 34 archivos; `DEPLOY_ROLLBACK` exige siete ejecuciones sobre un Deployment real y sus 14 regresiones pasan. Grupo productivo 87 casos (86 PASS/1 skip), backend 16/123 y Audit exit 0. No se ejerció cluster real; quedaba business acceptance.
- [x] auditoría ejecutable V113 — secure operations 1.1.0 materializa 37 archivos; `BUSINESS_ACCEPTANCE` fija GitHub Spec Kit 1.0.1 y Microsoft Playwright 1.62.1, exige cinco artefactos, escenario/efectos por capacidad, tres aprobaciones funcionales distintas, defectos gobernados y owner independiente. Sus 14 regresiones pasan y el grupo productivo termina 101 casos (100 PASS/1 skip). Los ocho productores semánticos existen; `PROJECT_CONDITIONED` se conserva exclusivamente porque un proyecto real debe aportar targets, cuentas, datos, personas y efectos.
- [x] auditoría ejecutable V114 — dos packs y tres owners ampliados entregan una única vertical de jornada de franquicia sin duplicar CRM/catálogo/pricing/pedidos/stock. `VERIFY_LIBRARY_PASS` gobierna 75 packs/756 archivos/445 Markdown y 36 perfiles; backend 17/135 pasa test/vet/build y PostgreSQL 18.6 up/tests/integración/down/up; web 6/75 pasa frozen offline, 25 tests, typecheck/build, Playwright 4+8 y Lighthouse cinco runs después de corregir el fallo SEO detectado. La memoria conserva 1.252 fallos locales y 202 condiciones upstream. Acciones autenticadas completas, reglas empresariales restantes y toda evidencia target/productiva siguen abiertas.
- [x] auditoría ejecutable V115 — el perfil web 6/78 incorpora un único command BFF y UI operativa sobre los endpoints Go existentes: Origin exacto antes de sesión, schema cerrado, organización+permiso, token server-only y cero precio/currency cliente. Frozen offline, 29 tests, typecheck, build, Playwright 4+8 y Lighthouse cinco runs PASS; `VERIFY_LIBRARY_PASS` gobierna 75/759/446 Markdown. Quote→order, handover UX, red/royalties/fiscalidad/financiación y todo target productivo siguen abiertos.
- [x] auditoría ejecutable V116 — backend 17/138 y web 6/80 convierten una quote autenticada en el único `sales.customer_order` dentro de la misma transacción, con línea server-priced, evidencia hash del comando y dos outbox; stale/cross-customer/expired/late-failure cierran sin efectos parciales. Markdown-only Go/PostgreSQL 18.6 y web frozen/30 tests/build/Playwright/Lighthouse PASS; handover UX, capacidad, red/royalties/fiscalidad/financiación y target productivo siguen abiertos.
- [x] auditoría ejecutable V117 — backend 17/141 y web 6/80 implementan un único calendario organization/kind: overlap serializado, slot exacto, capacidad bloqueada en transacción, exactamente un ganador concurrente, rollback sin residuo de idempotencia, selección pública server-owned y publicación protegida. Markdown-only Go/PostgreSQL 18.6, web frozen/32 tests/build, Playwright 4+8 y Lighthouse cinco runs PASS; recursos/empleados, cancelación/no-show, red/royalties/fiscalidad/financiación y target productivo siguen abiertos.
- [x] auditoría ejecutable V118 — backend 17/144 extiende el owner existente de `org.organization`/`franchise.agreement`: preflight de ciclos históricos, jerarquía serializada, parent/type/lifecycle/version fail-closed, cierre con dependientes bloqueado y territorios activos no solapados. Dos activaciones concurrentes producen exactamente un success/un conflict; migraciones 0001–0008, SQL invariants, down/up, full Go/vet/build y `VERIFY_LIBRARY_PASS` desde Markdown. Recursos/empleados, cancelación/no-show, royalties/settlement, fiscalidad/contabilidad, financiación y target productivo siguen abiertos.
- [x] auditoría ejecutable V119 — backend 17/147 añade `service_resource`, skills y assignment al calendario existente: principal obligatorio sólo para personas, organización/skill/estado exactos, lock por recurso, overlap rechazado, confirmación con asignación, completion/no-show sólo después del inicio y cancelación versionada. Markdown-only Go/PostgreSQL 18.6, 0001–0009 y down/up 0009 PASS. Working hours/leave/política laboral, royalties/settlement, fiscalidad/contabilidad, financiación y target productivo siguen abiertos.
- [x] auditoría ejecutable V120 — backend 18/156 añade un único owner `royalty`: política efectiva, pago captured/refunded exacto y versionado, cálculo minor-unit en PostgreSQL, devengo/lines/reconciliation inmutables, cierre serializable con exactamente un ganador, reversión igual/opuesta y diferencia externa explícita. Dos full Go consecutivos, vet/build, PostgreSQL 18.6 0001–0010, SQL y down/up PASS. Contratos/rates, provider payout/banco live, fiscalidad/contabilidad, financiación, UI y target productivo siguen abiertos.
- [x] auditoría ejecutable V121 — backend 19/165 añade un único owner `accounting`: cuentas/períodos, source-unique journal balanceado, posting serializable con exactamente un ganador, register/entries inmutables, trial balance scoped, reversión enlazada y cierre que bloquea drafts. Dos full Go consecutivos, vet/build, PostgreSQL 18.6 0001–0011, SQL y down/up PASS. Localización fiscal/estatutaria, tax/e-invoice, bancos/FX/consolidación, financiación, UI y target productivo siguen abiertos.
- [x] auditoría ejecutable V122 — pack ARCA WSAA 0.1.0 materializa 7/7: cinco textos oficiales declarados `ADAPTED` sólo por CRLF→LF y dos scripts locales. La adquisición offline verifica dos ZIP oficiales, sus tamaños/SHA-256 y cinco hashes originales; el test materializado preserva términos, parser y condiciones legacy. No se promueve como adapter moderno: credenciales/certificados, WSFE actual, reglas fiscales, homologación y reconciliación target siguen abiertos.
- [x] auditoría ejecutable V123 — pack Microsoft/ARCA WSFE 0.1.0 materializa 6/6 y genera proxy desde WSDL público exacto sin incorporar contrato/proxy. .NET 10.0.400, svcutil 8.0.0, WCF 10.0.652802 y crypto 10.0.11 quedan separados; homologación y producción-metadata producen hashes exactos, build 0 warnings y NuGet vulnerable clean. WSAA moderno, claves/certificados, reglas/estado fiscal, reconciliación y homologación target siguen abiertos.
- [x] auditoría ejecutable V124 — el mismo pack sube a 0.2.0 y, sin duplicar runner/toolchain, genera WSAA+WSFEv1 en namespaces separados. Los dos WSDL por servicio sólo difieren por endpoint; cuatro hashes generados, dos ambientes, build warning-free y scan NuGet limpio PASS. Credenciales/custodia/ticket, reglas fiscales, estado durable, CAE reconciliation y homologación real siguen abiertos.
- [x] auditoría ejecutable V125 — credential core 0.1.0 materializa 18/18 con cuatro locks: request `wsfe`/12h, CMS attached verificado, certificate store exacto, parser 1 MiB/DTD-off, cache memory single-flight y adapter al `LoginCMS` generado. Nueve core + tres integración, 32 concurrentes→1 llamada, warning-free/NuGet clean, secrets/manual glue 0 PASS. Certificado/asociación/homologación y dominio fiscal durable siguen abiertos.
- [x] auditoría ejecutable V126 — backend 20/176 añade owner `fiscal` sin duplicar pedidos, pagos, accounting ni outbox: CUIT checksum, importe/composición exactos, payment captured, lane por punto/tipo, secuencia oficial, timeout→`reconcile_required`, consulta antes de reenvío, CAE e intentos inmutables. Full Go repetido/vet/build, PostgreSQL 18.6 0001–0012, SQL, down/up y focal repetido PASS. Adapter SOAP generado, certificado/asociación/punto de venta de homologación, parámetros y reglas tributarias/QR/PDF/notas aprobadas continúan abiertos.
- [x] auditoría ejecutable V127 — el proxy generado demostró que agregados solos no pueden construir `AlicIva[]`/`Tributo[]`; fiscal 0.2.0 conserva líneas exactas, exige sumas/bases coincidentes mediante servicio y constraints diferidos, vuelve detalles inmutables y recarga replay/claim/read sin pérdida. Perfil 20/176 materializado, full Go dos veces/vet/build, PostgreSQL 18.6 down/up/SQL y focal dos ejecuciones reales PASS. Adapter SOAP, parámetros aprobados y homologación siguen abiertos.
- [x] auditoría ejecutable V128 — manual ARCA 4.6 revisión 4.0/error 10246 y proxy generado revelaron `CondicionIVAReceptorId` obligatoria; fiscal 0.3.0 la exige/persiste/relee sin inferirla. Perfil 20/176 materializado; full Go dos veces/vet/build y PostgreSQL 18.6 down/up/SQL/focal dos ejecuciones PASS. Adapter SOAP, tabla paramétrica aprobada y homologación continúan abiertos.
- [x] auditoría ejecutable V129 — adapter SOAP 0.1.0 materializa 10/10 y compone assets generador 4 + credential core 8 + adapter 10 dentro de backend 23/198. .NET 10.0.400 locked/offline, build 0 warnings, 12 contratos, NuGet current clean y Go test/vet/build PASS; error 602 es única consulta negativa admitida y ningún secreto cruza el DTO. IPC Go↔.NET y evidencia live permanecen abiertos.
- [x] auditoría ejecutable V130 — worker UDS 0.1.0 materializa 13/13 y aplicación 1.4.0 agrega dos archivos CSPRNG compartidos sin duplicar el generador; backend 24/213 compone desde Markdown. Go full test dos veces/vet/dos builds, Kestrel real por `AddressFamily.Unix`, ocho contratos, .NET locked-offline/build 0 warnings y NuGet current clean PASS. IPC local queda cerrado; certificado/asociación, homologación live, parámetros fiscales y aprobación contable siguen condicionados.
- [x] auditoría ejecutable V131 — adapter SOAP 0.2.0 materializa 14/14 y consulta siete familias oficiales mediante el proxy generado: comprobante, concepto, documento, IVA, tributo, punto de venta y condición IVA receptor. Quince contratos paramétricos más doce de comprobante, builds locked-offline sin warnings, scan NuGet current y backend Markdown 24/217 con Go full dos veces/vet/dos builds PASS. No se embebe tabla fiscal ni se atribuye el glue a ARCA/Microsoft; certificado/asociación, homologación live, persistencia/refresco y política fiscal aprobada siguen condicionados.
- [x] auditoría ejecutable V133 — fiscal 0.5.0 materializa 25/25 y worker UDS 0.2.0 13/13; siete scopes atraviesan Go↔UDS↔Kestrel↔bridge y se guardan por organización/CUIT/hash. API OIDC separa read/refresh/configure/approve, scheduler usa lease/retry y ninguna adquisición autoaprueba. Backend Markdown 24/232 pasó full Go y PostgreSQL 18.6 0001–0014; .NET locked/12 UDS permanece verificado. Certificado/asociación, homologación y aprobación fiscal target siguen condicionados.
- [x] auditoría ejecutable V134 — journey 0.5.0 materializa 24/24 y el backend Markdown 24/238 pasa full Go/vet/build y PostgreSQL 18.6 0001–0015. Horario de organización habilita cupos; horario+skill+ausencia+no-overlap gobiernan recurso; concurrencia conserva un ganador; actor/motivo quedan inmutables; el cliente sólo cancela su turno futuro con versión vigente. Recurrencia/timezone/política y UX web target siguen condicionados.
- [x] auditoría ejecutable V135 — portal journey 0.5.0 materializa 12/12; perfil web Markdown 6/82 pasa frozen offline, 34 tests, typecheck, build y licencia. El BFF estricto mapea availability/resource/assignment/reason transition/customer cancel sin bearer ni customer subject en browser y rechaza campos server-owned. IdP/backend/edge/política y browser protegido target siguen condicionados.
- [x] auditoría ejecutable V136 — journey backend 0.5.1 conserva 24/238 y portal 0.6.0 lleva web a 6/84. PostgreSQL real rechaza serie incorrecta y acepta sólo customer/version/serie durable; servidor calcula el hash del comando y outbox atomiza. Full Go/vet/build y web frozen/35/typecheck/build/licencia pasan desde Markdown. Firma/checklist/legal target siguen condicionados.
- [x] auditoría ejecutable V138 — journey 0.6.0 materializa 27/27 y backend 24/241; portal 0.7.0 conserva web 6/84. PostgreSQL real 0001–0016 rechaza aceptación previa, respuesta obligatoria ausente, versión checklist distinta, serie incorrecta y customer cruzado; publicación/completion/outbox son transaccionales e inmutables. Full Go/vet/cuatro builds y web frozen/36/typecheck/build/licencia pasan desde Markdown. Contenido, roles, retención/firma y target productivo siguen condicionados.
- [x] auditoría ejecutable V139 — journey 0.7.0 materializa 30/30 y backend 24/244; portal 0.8.0 conserva web 6/84. PostgreSQL real 0001–0017 demuestra rechazo customer-scoped, excepción durable, sucesor de corrección y autorización de devolución ligada a pedido/stock/fuente de costo e inmutable; no simula efectos posteriores. Full Go/vet/cuatro builds y web frozen/37/typecheck/build/licencia pasan desde Markdown. Recepción física, inventory disposition, refund/exchange, posting contable/fiscal y target productivo siguen condicionados.
- [x] auditoría ejecutable V140 — journey 0.8.0 materializa 33/33 y backend 24/247; portal 0.9.0 conserva web 6/84. PostgreSQL real 0001–0018 rechaza serie, duplicados y mutación; receipt/disposition crean exactamente solicitudes inventory + refund/exchange + accounting + fiscal según autorización. Full Go/vet/cuatro builds y web frozen/38/typecheck/build/licencia pasan desde Markdown. Consumers provider-specific, reconciliación, posting real y target productivo siguen condicionados.
- [x] auditoría ejecutable V141 — return-effect worker 0.1.0 materializa 8/8 y backend 25/255. Full Go/vet/cinco builds y PostgreSQL real 0001–0019/down-up pasan desde Markdown; concurrencia, wrong-worker, fencing, duplicate suppression, blocked-resume, audit inmutable y stock+outbox+resultado atómicos quedan demostrados. Refund/exchange/accounting/fiscal, provider reconciliation, exact-cost posting y target productivo siguen condicionados.
- [x] auditoría ejecutable V142 — official return-refund worker 0.1.0 materializa 14/14 y backend 26/269. SDKs oficiales Stripe 86.3.0/Mercado Pago 1.14.0 ejecutan create/retrieve con idempotencia; módulo frozen/test/vet/build, backend completo y PostgreSQL 18.6 0001–0020/down-up pasan desde Markdown. Integración demuestra dos parciales→total exacto, outbox, evidencia inmutable y ambiguity block. Cuentas/webhooks live, políticas discount/tax/shipping, exchange/accounting/fiscal y target productivo siguen condicionados.
- [x] auditoría ejecutable V143 — return exchange fulfillment worker 0.1.0 materializa 8/8 y backend 27/277. Full Go/vet/seis root builds y PostgreSQL 18.6 0001–0021/down-up pasan desde Markdown. Integración demuestra pedido de reemplazo independiente, reserva exacta/atómica, handover preparado, outbox/audit terminal, stock único y retry sin disponibilidad. Diferencia de variante/precio, cross-location/carrier, accounting/fiscal, entrega/aceptación y target productivo siguen condicionados.
- [x] auditoría ejecutable V144 — return accounting reversal worker 0.1.0 materializa 8/8 y backend 28/285. Go/vet/build y PostgreSQL 18.6 0001–0022 pasan; integración prueba reversión igual/opuesta del journal original, vínculo inmutable y recuperación tras commit previo sin duplicar. Aprobación contable, exact-cost/tax del asiento original, fiscal ARCA y target siguen condicionados.
- [x] auditoría ejecutable V145 — fiscal owner 0.6.0, SOAP 0.3.0 y UDS 0.3.0 materializan 28/28 + 14/14 + 13/13; backend 28/290 incluye el generador. Proxies WSFE hash-locked, Go/vet/siete builds, 15+15 contratos bridge, 12 UDS y PostgreSQL 18.6 0001–0023/integración pasan desde Markdown. Creación automática de la nota de crédito y homologación live siguen condicionadas.
- [x] auditoría ejecutable V146 — return-fiscal credit-note worker 0.1.0 materializa 8/8 y backend 29/298. Full Go/vet/ocho builds y PostgreSQL 18.6 limpio 0001–0024/SQL/integración pasan desde Markdown. La integración prueba una sola nota, pending→authorized, dos intentos, vínculo inmutable y replay sin duplicado; homologación, notas parciales/débito/rendering y aprobación fiscal live siguen condicionadas.
- [x] auditoría ejecutable V147 — Amazon Shipping tracking 0.1.0 materializa 7/7 y compone con el lock oficial en 2/32. `amzn-sp-api` 1.11.1 exacto, `pip check`, firma Shipping V2 y seis pruebas pasan; el receipt hashea tracking ID y no completa entregas. Cuenta/rol/región/carrier/sandbox/reconciliación live y carrier universal siguen condicionados.
- [x] auditoría ejecutable V148 — Amazon Shipping 0.2.0 materializa 10/10 y compone 2/35. Once pruebas construyen modelos oficiales Address/Dimensions/Weight/Currency/Item/Package/Channel/GetRatesRequest, rechazan inputs/efectos ambiguos y fijan `shipment_purchased=false`; un paquete se conserva por issue oficial #5270. Purchase/cancel/docs y live siguen condicionados.
- [x] auditoría ejecutable V149 — Amazon Shipping 0.3.0 materializa 13/13 y compone 2/38. Dieciséis pruebas construyen PurchaseShipmentRequest/RequestedDocumentSpecification oficiales, ligan quote/receipt/rate/cargo/documentos dentro de diez minutos, persisten idempotency key antes del POST y dejan `UNKNOWN_EFFECT` sin retry automático. Cuenta/sandbox/reconciliación live, cancel/docs/handover y producción siguen condicionados.
- [x] auditoría ejecutable V150 — Amazon Shipping 0.4.0 materializa 16/16 y compone 2/41. Veintidós pruebas atan cancelación al purchase exacto, persisten attempt antes del PUT sin idempotency key, bloquean retry ambiguo y recuperan documentos oficiales con binding shipment/package/selection, Base64/límites/hash/commit atómico. Cuenta/sandbox/reconciliación/retención live, handover/pickup/claims y producción siguen condicionados.
- [x] auditoría ejecutable V151 — Amazon Shipping 0.5.0 materializa 20/20 y compone 2/45. Veintiocho pruebas separan inventory read-only de generate, ligan approval a carrier/address oficial, persisten idempotency key, validan PDF Base64/hash y fijan `physical_handover_completed=false`. Cuenta/sandbox/reconciliación live, pickup/escaneo/firma/claims y producción siguen condicionados.
- [x] auditoría ejecutable V152 — Amazon Shipping 0.6.0 materializa 23/23 y compone 2/48. Treinta y tres pruebas atan claim a purchase/quote/rate-request/selection, limitan valor a insured value, restringen proof hosts y conservan `UNKNOWN_EFFECT` sin retry porque createClaim no expone idempotency key. Cuenta/sandbox/legal/settlement/reconciliación live, pickup/escaneo/firma y producción siguen condicionados.
- [x] auditoría ejecutable V153 — Amazon Easy Ship 0.1.0 materializa 9/9 y compone 2/34. Nueve pruebas con el wheel oficial exacto demuestran slots únicos/futuros, schedule aprobado y hash-bound, attempt antes del POST, `UNKNOWN_EFFECT` sin retry, reconciliación `PickedUp` y no-overwrite; `automatic_internal_handover_completion=false` queda fijo. Cuenta/rol/support table/sandbox/live, cancelación inequívoca, firma/foto, carrier universal/cross-location y producción siguen condicionados.
- [x] auditoría ejecutable V154 — Amazon Fulfillment delivery evidence 0.1.0 materializa 8/8 y compone 2/33. Doce pruebas con el wheel oficial exacto demuestran firma/modelos, perfil bloqueado, binding receipt/pedido/marketplace/paquete, documento único, host/status, límites+magic bytes, hashes, redacción de URL firmada y no-overwrite inicial/concurrente; identidad, tracking y URLs no se persisten y `automatic_business_delivery_acceptance=false` queda fijo. Cuenta/rol/MCF/sandbox/live, listas observadas, privacidad/retención, aceptación y producción siguen condicionados.
- [x] auditoría ejecutable V155 — Amazon Supply Sources 0.1.0 materializa 9/9 y compone 2/34. Catorce pruebas con el wheel oficial exacto demuestran list/get paginado, ciclo/duplicados, filtrado de ubicación/contacto, modelos create/update/status, archive sólo Inactive, approval/snapshot/drift, attempt antes del efecto, reconciliación, `UNKNOWN_EFFECT` sin retry y no-overwrite; `automatic_internal_location_activation=false` queda fijo. Cuenta/rol/programa/dynamic sandbox/live, inventario/promesas cross-location internos y producción siguen condicionados.
- [x] auditoría ejecutable V156 — Amazon MLI inventory 0.1.0 materializa 9/9 y compone 3/43. Trece pruebas con el wheel oficial exacto demuestran GET `fulfillmentAvailability`, receipt Supply Sources Active, request y approval estrictos, drift, `ListingsItemPatchRequest(PRODUCT)`, replace de `/attributes/fulfillment_availability`, attempt previo, rechazo sanitizado, reconciliación, `UNKNOWN_EFFECT` sin retry, no-overwrite y cero IDs crudos en outputs; `automatic_internal_inventory_commit=false` queda fijo. Cuenta/rol Product Listing/inscripción MLI/dynamic sandbox/live, red interna universal y producción siguen condicionados.
- [x] auditoría ejecutable V157 — Amazon External Fulfillment Inventory 0.1.0 materializa 9/9 y compone 2/34. Doce pruebas con el wheel oficial exacto demuestran batch máximo 10, canales oficiales, URI codificada, modelos `BatchInventory*`, FETCH filtrado, sequence por identidad, approval/drift/attempt, rechazo total sanitizado, partial/exception `UNKNOWN_EFFECT`, reconciliación por sequence, no-overwrite y cero IDs crudos; acknowledgement nunca hace commit ERP/POS/WMS. Cuenta/programa/rol/mapping/sequence/sandbox/live y producción siguen condicionados.
- [x] reauditoría V158 — acquisition core 0.4.76 y Business Central platform 0.1.1 fijan `microsoft/BCApps@2eae56d` firmado: 36.673 AL, Inventory 958, Warehouse 355, SCM-Reservation 22 AL y manifests exactos Availability/Tracking/Warehouse/tests. Los packs reconstruidos pasan lock 121/121, 9 negativos, 16 perfiles/5 negativos/1 positivo y runtime config 1/2 negativos/1 regresión. La release firmada 28.4 StrictMode queda correctamente separada porque omite BaseApp; contenedor y suites AL siguen condicionados.
- [x] reconstrucción V159 — supply 0.3.0 materializa 15/15 y backend 29/307. Seis archivos `ADAPTED` trasladan de forma estrecha invariantes Microsoft BCApps de reserva/ATP/transferencia a Go/PostgreSQL; tres archivos de derivación/HTTP son `AUTHORED`. Full Go/vet/dos builds y PostgreSQL 18.6 limpio 0001–0025 pasan, incluida carrera con un ganador, reserva de comercio unificada, ATP en tránsito, recepción por destino y retransferencia. No equivale a planning/CTP, bulk/lot/bin/costing ni producción.
- [x] reconstrucción V160 — supply 0.4.0 materializa 24/24, application 1.7.0 materializa 5/5 y backend 29/316. Nueve bloques nuevos trasladan de forma declarada invariantes BCApps de lot/bin/availability/item application: 6 `ADAPTED`, 3 `AUTHORED`. Desde Markdown pasan 0001–0026, full Go/vet/dos builds, carrera cuantitativa, FIFO 740/2 capas, específico 999.99, ledger SQLSTATE 55000, movimiento y cleanup aislado. Planning/CTP, average/standard/LIFO, landed-cost/G-L/períodos, bulk cross-org, picks/put-aways y producción siguen condicionados.
- [x] reconstrucción V161 — supply 0.5.0 materializa 33/33, application 1.8.0 materializa 5/5 y backend 29/325. Nueve bloques nuevos trasladan de forma declarada invariantes BCApps de receipt/activity/bin ranking/put-away/pick/FEFO: 6 `ADAPTED`, 3 `AUTHORED`. Desde Markdown pasan 0001–0027, full Go/vet/dos builds, ranking/capacidad, exclusión de bins, FEFO 4+3, una sola carrera ganadora, traslado reservado a SHIP, cancelación y SQLSTATE 55000. Bulk cross-org, partial handling/breakbulk/cross-docking/replenishment, planning/CTP y producción siguen condicionados.
- [x] reconstrucción V162 — supply 0.6.0 materializa 42/42, application 1.9.0 materializa 5/5, journey 0.8.1 corrige teardown repetible y backend 29/334. Nueve bloques nuevos trasladan invariantes fijadas de Microsoft BCApps transfer header/line/post shipment/post receipt: 6 `ADAPTED`, 3 `AUTHORED`. Desde Markdown pasan 0001–0028, down/up 0028, full Go/vet/dos builds y PostgreSQL real: replay idempotente, pick transfer-outbound, carrera de shipment con un ganador, tránsito 3/30.0000, lote inmutable, receipt/put-away destino, conservación 5=2+3, cancelación que libera reservation y no puede despachar. Specific-cost transfer, partial handling/breakbulk/cross-docking/replenishment, planning/CTP y producción siguen condicionados.
- [x] reconstrucción V163 — supply 0.7.0 materializa 44/44 y backend 29/336 desde Markdown. Dos migraciones `ADAPTED` 0029 agregan selector exacto de receipt para costo específico; falta de selector, selector en FIFO y replay divergente fallan cerrados. PostgreSQL 18.6 limpio pasó 0001–0029, down/up 0029, dos corridas focales, suite completa, vet y ambos builds: entre capas 11/17 consumió sólo 17, preservó la primera en 1.000000, recibió 17.0000 y corrigió `lot_id NULL` sin romper FIFO/lote/cancelación. Partial handling/breakbulk/cross-docking/replenishment, planning/CTP, contabilidad avanzada y producción siguen condicionados.
- [x] reconstrucción V164 — supply 0.8.0 materializa 46/46 y backend 29/338 desde Markdown. Dos migraciones `ADAPTED` 0030 añaden posting durable e idempotente de shipment/receipt parcial sobre cantidades acumuladas oficiales BCApps. PostgreSQL 18.6 limpio pasó 0001–0030, down/up 0030, dos corridas focales, suite completa, vet y `cmd/...`: dos shipments/tres receipts, un ganador concurrente, replay exacto, tránsito 2→1→0, costo 20→10→0, lote, tres put-away y costo específico 17 sin tocar 11. `VERIFY_LIBRARY_PASS` confirma 94/1055/520/backend338 y `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit` confirma 121 fuentes. Breakbulk/cross-docking/replenishment, planning/CTP, contabilidad avanzada, target y producción siguen condicionados.
- [x] reconstrucción V165 — supply 0.9.0 materializa 52/52 y backend 29/344 desde Markdown. Cinco archivos `ADAPTED` y un record `AUTHORED` fijan BCApps `2eae56d`: target fijo PICK/PUTPICK bajo mínimo, máximo menos stock/pending, fuentes lower-rank netas, bloqueos/vencimiento, FEFO, reservas y movement register/cancel. PostgreSQL 18.6 limpio pasó 0001–0031, focal dos veces, full Go/vet/build, down/up 0031 y focal posterior: 5+3 hasta 10, replay exacto/divergente, cuatro pares de entries, target lleno rechazado, cancelación multifuente y un ganador concurrente. `VERIFY_LIBRARY_PASS` confirma 94/1061/521/backend344 y `VERIFY_EXECUTABLE_LIBRARY_PASS mode=Audit` confirma 121 fuentes, incluyendo tres adapters Go con 1.26.7 explícito. Breakbulk/cross-docking, planning/CTP, WMS externo, target y producción siguen condicionados.
- [x] reconstrucción V166 — supply 0.10.0 materializa 58/58 y backend 29/350 desde Markdown. Cinco archivos `ADAPTED` y un record `AUTHORED` fijan seis owners/tests BCApps de cross-dock y Microsoft Learn. PostgreSQL 18.6 limpio pasó 0001–0032, focal repetido, cross-dock+transfer dos veces, full Go/vet/build y down/up 0032 con focal posterior: invalid bin/policy, generic pick 2, split 3+4, segunda recepción sin duplicar, no pick antes de put-away, over-demand rechazado, cancelación/retry y estado final reserved 0/picked 3/cross 0/ship 3. Reserva parcial usa identidad source+activity+sequence sin debilitar el índice único. El gate raíz final gobierna 94/1067/522/backend350. Breakbulk/UOM, calendarios, sales/service/production cross-dock, planning/CTP, WMS externo, target y producción siguen condicionados.

Actualización V327: EXECUTION-VALIDATOR1.3.1 conserva15archivos y schemas;27tests PASS incluyen diagnóstico controlado de errores OS/Unicode de ambos CLI. Guía NEW/EXISTING13comandos/8helps PASS; entradas V256/V257 anteriores conservan su snapshot1.3.0/24tests histórico. No promoción ni cierre de TEST08 sobre el release final. Ver reconstruction_evidence/CLI_ENTRY_DIAGNOSTICS_V327.md.

V328: readiness0.6.2,75tests/8files. Diagnóstico CLI controlado y publicación de
reportes sin reemplazo tardío probados; schemas/prompts/validate_record intactos.
Snapshot V2830.6.1/70tests preservado. No cierre de readiness real42bloqueos ni
de usabilidad/release. Ver READINESS_REPORT_PUBLICATION_V328.md.

V355: GO-REMINDERS-CORE0.2.0 exige Due(tenant)/MarkSent(tenant,id) y claves tenant/ID;3fallos de aislamiento reproducidos,5aserciones originales preservadas con nuevos argumentos.2fuentes exactas,11tests x3,vet/build y fuzz10s/4workers/2112907ejecuciones PASS. Caller0.1.x rechazado al compilar; no shim global ni promesa de envío único. Waitlist0.1.1 sólo retira compatibilidad insegura, fuentes intactas. TEST02/03parciales,43/48sin cambio; ambos CONDITIONED/fuera del perfil. Evidencia reconstruction_evidence/REMINDER_TENANT_API_V355.md.
