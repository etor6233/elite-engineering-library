# V403 cloud execution extension — T2801/T2808/T2809/T2810

New extension over immutable V402/337. This plan coordinates source/worker/cloud infrastructure; it is not another product roadmap. Canonical entry is [the complete120pack extension](FRANCHISE_COMPLETE_EXTENSION_PACK_PLAN_V403.md), with the unchanged first116selections and four explicitly versioned extensions. Production remains unauthorized.

The new cloud pack contains the complete source/profile consumer seal, worker tooling acquisition, original-owner build invocation, five exact-output OCI packaging recipes, private browser/API identity wiring, full resource DAG, bounded approved execution with durable intent, scheduled billing observation/CAS journal/alert dispatch plus separately approved restriction contracts and behavioral fixtures. It does not carry acquired runtimes, website assets, credentials or signed V402 ZIPs. The materialized `cloud/README.md` gives the exact sequence and `cloud/ADMISSION.md` separates local claims from new target dependency admission.

```json
{
  "schemaVersion": "1.0.0",
  "name": "franchise-cloud-execution-v403",
  "description": "Isolated new cloud control/configuration extension; use the complete120plan for connected composition",
  "packs": [
    {
      "path": "implementation_packs/FRANCHISE_CLOUD_EXECUTION_V403.md",
      "packId": "FRANCHISE-CLOUD-EXECUTION-V403",
      "version": "0.1.0",
      "acknowledgeConditions": true,
      "files": ["*"],
      "variables": {}
    }
  ]
}
```

The standalone selection above is for inspection/materialization of this pack. Running it alone does not install the full116runtime, C owners or UI overlay engine. The complete plan is required for connected use.

The library is a separate repository pinned to a chosen exact Git commit by `cloud/seal_consumer.py`, which writes the lock in the consumer. It reads selected files from Git objects, not a dirty working tree, and avoids embedding this pack's own future SHA or commit. `d2729ab` remains the original116plusconnection qualification; availability of any later local commit on the public remote requires the separate user-approved push. No force or public-history rewrite is authorized.

Verification is scoped: original Windows local admission is preserved; Linux full116checkout/materialization and a synthetic .NET certificate probe were actually executed. New V403fixtures exercise the resource DAG/approval/reconciliation adapters without providers. Full Linux OCI builds, target SCA/runtime, provider execution, hosted CI, target IAP/OIDC/browser journey, billing/alerts/effective restriction, stop/recovery and real product acceptance remain explicitly NOT_RUN or pending their exact prerequisite; missing technical work is never renamed a missing credential. The previous signed Windows artifact cannot be relabeled Linux or rebuilt under the original published ZIP name/SHA.

The selected API embeds background loops, requiring CPU always/min1until an explicit drained suspension. IAP_DIRECT protects the web URL while workload IAM and app OIDC stay separate. ARCA's homologation worker remains unchanged; a new Linux init adapter imports readonly mounted certificates into CurrentUser/My and invokes the original Main. The final Daybreak/native deferred case stays untouched.

Local evidence: the external extension's `cloud/evidence/qualification-*.json`, `linux-full-bootstrap.json`, stack receipts and `pack-manifest.json`, linked by the root V403integration receipt. Each names its exact source hashes and actual method; no unexecuted test is marked PASS.
